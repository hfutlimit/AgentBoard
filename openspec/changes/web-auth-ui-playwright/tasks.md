# Tasks

- [ ] SPA：头部鉴权区与登录/注册弹窗（`index.html` + `app.js`）
- [ ] SPA：localStorage token 持久化 + `api()` 附带 `Authorization` + `/api/auth/me` 显示用户名
- [ ] 样式：`#modal` / `.modal-box`
- [ ] 新增依赖 `playwright`（`requirements.txt` / 文档）
- [ ] Playwright 测试：启动 api + web，注册/登录（含错误分支）页面覆盖
- [ ] Playwright 测试：`project → epic → story → task/bug` 创建/修改/列表读取页面覆盖
- [ ] 运行测试确认通过；归档变更并更新 `specs/agentboard/spec.md`
