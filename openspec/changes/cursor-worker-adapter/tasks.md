# Tasks

- [x] `CursorAdapter` + `CliLocator.LocateCursor` + JSON `.result` 抽取
- [x] `AgentsOptions.Cursor`、DI、startup slot、ReadinessProbe
- [x] Worker-owned 白名单、factory、catalog、配置台
- [x] 适配器 / catalog / portal 测试
- [x] 本机配置 Grok 4.6 Agent 并启动 Worker
