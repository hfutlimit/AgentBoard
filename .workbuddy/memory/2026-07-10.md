# 2026-07-10 工作日志

## 需求文档 + 任务拆分（OpenSpec 规范驱动）

按用户要求，为 4 项新能力补充需求文档与可勾选任务清单，供后续自动任务处理。

### 关键发现（现状盘点）
- 后端鉴权已 100% 就绪：`models.User`、`auth.py`（pbkdf2+HMAC）、`service.register_user/authenticate_user/get_user`、`api.py` 的 `/api/auth/register|login|me`、`users` 表 Alembic 迁移、后端测试。
- 缺：前端 SPA 登录/注册 UI、token 持久化、受保护路由。
- MariaDB：URL 切换 / pymysql / Alembic 迁移（含 users）/ docker-compose `db` profile 已就绪；缺独立 `.sql` 脚本与真实验证。
- Playwright：无，现有 `test_web_flow.py` 仅用 httpx 模拟。
- MCP：服务已完整实现（api/db 双后端、全套工具）；"实现MCP" 解读为鉴权集成 + 运维化。

### 产出文件
- `docs/requirements.md`：新增 FR-8~FR-11、更新范围/开放问题。
- `openspec/changes/auth/`：前端登录注册集成（proposal/design/tasks）。
- `openspec/changes/mariadb-alembic/`：扩展为含独立 `.sql` 脚本 + 集成验证（已更新 proposal/design/tasks）。
- `openspec/changes/playwright-e2e/`：Playwright 真实浏览器 E2E（新增）。
- `openspec/changes/mcp-auth/`：MCP 鉴权工具 + token 透传 + 启动脚本/客户端配置/冒烟测试（新增）。
- `docs/tasks.md`：追加 Epic 7~10，关联上述 changes。

### 设计决策
- "实现MCP" 解读为：新增 `auth_register/auth_login/auth_me` 工具、`AGENTBOARD_MCP_TOKEN` 透传、客户端配置样例、MCP 冒烟测试，使现有服务生产可用。
- 现有 CRUD 默认保持单用户开放；是否强制鉴权由 `auth` 变更的 `AGENTBOARD_REQUIRE_AUTH` 开关决定（默认关）。
