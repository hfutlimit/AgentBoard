# 2026-07-10 工作日志

## 需求文档 + 任务拆分（OpenSpec 规范驱动）

按用户要求，为 4 项新能力补充需求文档与可勾选任务清单，供后续自动任务处理。

### 关键发现（现状盘点）
- 后端鉴权已 100% 就绪：`models.User`、`auth.py`（pbkdf2+HMAC）、`service.register_user/authenticate_user/get_user`、`api.py` 的 `/api/auth/register|login|me`、`users` 表 Alembic 迁移、后端测试。
- 缺：前端 SPA 登录/注册 UI、token 持久化、受保护路由。
- MariaDB：URL 切换 / pymysql / Alembic 迁移（含 users）/ docker-compose `db` profile 已就绪；缺独立 `.sql` 脚本与真实验证。
- Playwright：无，现有 `test_web_flow.py` 仅用 httpx 模拟。
- MCP：服务已完整实现（api/db 双后端、全套工具）；"实现MCP" 解读为鉴权集成 + 运维化。

### 产出文件
- `docs/requirements.md`：新增 FR-8~FR-11、更新范围/开放问题。
- `openspec/changes/auth/`：前端登录注册集成（proposal/design/tasks）。
- `openspec/changes/mariadb-alembic/`：扩展为含独立 `.sql` 脚本 + 集成验证（已更新 proposal/design/tasks）。
- `openspec/changes/playwright-e2e/`：Playwright 真实浏览器 E2E（新增）。
- `openspec/changes/mcp-auth/`：MCP 鉴权工具 + token 透传 + 启动脚本/客户端配置/冒烟测试（新增）。
- `docs/tasks.md`：追加 Epic 7~10，关联上述 changes。

### 设计决策
- "实现MCP" 解读为：新增 `auth_register/auth_login/auth_me` 工具、`AGENTBOARD_MCP_TOKEN` 透传、客户端配置样例、MCP 冒烟测试，使现有服务生产可用。
- 现有 CRUD 默认保持单用户开放；是否强制鉴权由 `auth` 变更的 `AGENTBOARD_REQUIRE_AUTH` 开关决定（默认关）。

## 新增长期任务：持续前端优化（模仿 Jira，小步迭代）

用户追加要求：把"持续优化前端页面、持续新增 feature、模仿 Jira、每次只做小的优化"作为长期任务纳入规划。

### 产出
- `docs/requirements.md`：新增 FR-12（长期前端优化轨道）。
- `docs/tasks.md`：新增 **Epic 11**（迭代规则 R1~R5 + Backlog A 纯前端 20 项 + Backlog B 需后端 6 项 + 完成记录表）。
- `openspec/changes/frontend-continuous/`：proposal/design/tasks（process 容器，backlog 以 Epic 11 为权威源）。
- 顺手修正了 tasks.md 待办区误把 Epic 7~10 标 `[x]` 的问题（改为 `[ ]`，与详细 Epic 节一致）。

### 关键约定（可复用）
- 迭代纪律：每周期只做一项小优化；单文件改动为主、新增 < ~80 行、不引入新框架/依赖、不改后端契约。
- 纯前端 Backlog A 可直接做；需后端字段的（labels/assignee/due_date/拖拽/评论）归入 Backlog B，单独提 change 评估。
- commit 规范：`feat(ui): 前端小优化 - <描述>`。

## 在系统里初始化两个项目
- 用户已建好自动化任务，要求先在系统初始化两个项目：**DevPolit**（`key=DEV`，`id=1`）、**ChessPolit**（`key=CHESS`，`id=2`）。
- 用项目自身 `service.create_project` + 默认 SQLite（仓库根 `./agentboard.db`）创建；`init_db()` 跑通 Alembic 迁移（initial + users 表）。脚本放系统临时目录、未进仓库，事后已清理。
- 数据库被 `.gitignore` 忽略（`*.db`），属运行时数据，不提交；故本步无 git 变更。
- 注意：用户拼写为 "DevPolit"/"ChessPolit"（疑似 DevPilot/ChessPilot 笔误），按原样创建，已提示可改名。若系统实际跑 MariaDB（设 `AGENTBOARD_DB_URL`），需在该库重跑初始化。

## 项目硬性 Git 约定（用户明确要求）
- "这个项目里记住 每次修改 都要及时 push" → 已写入 `MEMORY.md` 协作流程约定。
- 含义：本项目任何文件变更（含文档/任务拆分），完成 commit 后**必须立即 `git push origin main`**；push 失败须提示用户本地重试，不得静默跳过。
