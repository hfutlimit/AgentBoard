# 2026-07-12 工作日志

## Epic 7 前端登录/注册 UI（鉴权最后一公里）— 完成并交付
- **背景**：自动化周期执行。Epic 11 纯前端 backlog（A-01~A-20、B-05/B-06、P-01~P-15）已全部完成；下一个高价值 pending 是 Epic 7（后端鉴权已 100% 就绪）。
- **关键发现**：`docker-compose.yml` 的 api 服务写死 `AGENTBOARD_REQUIRE_AUTH: "1"`，但**运行中的 api 容器实际未强制**（实测 `/api/projects` 返回 200），即线上 API 当前是开放的。因此若做成"无 token 硬拦截进入"会破坏现有开放部署。
- **设计决策（动态守卫）**：`api()` 收到 401 自动跳登录（auth 端点自身不触发，避免递归）；启动用 `/api/auth/me` 校验登录态；后端开放时免登可用、置 `AGENTBOARD_REQUIRE_AUTH=1` 时自动跳登录。SPA 不改造成硬 gate。
- **改动**：`app.js`(+123/−3) 新增 `getToken/setToken/clearToken`+`CURRENT_USER`/`_AUTH_VISIBLE`/`_AUTH_MODE`、`showAuthScreen()/authScreenHTML()/bindAuthScreen()`（登录/注册 tab 切换、调 `/api/auth/register|login`、成功存 token 失败 toast）、`updateUserInfo()` 顶栏用户名+登出、`logout()`、`startApp()`；`render()` 加 `_AUTH_VISIBLE` 守卫；启动 IIFE 改为先校验 token 再 `startApp()`。`style.css`(+20) 补 `.auth-wrap/.auth-card/.auth-tabs/.auth-form/.user-chip` 等。`test_web_flow.py`(+12/−1) 增 Epic 7 静态断言并将 `css` 获取前移（修复 `UnboundLocalError`）。`docs/tasks.md` 勾选 Epic 7 三项 Story + 完成记录。
- **测试**：托管 venv 跑 `tests/test_web_flow.py`+`tests/test_backend_flow.py` → **6 passed**（新增 Epic 7 断言）。
- **部署**：Docker Hub 不可达（`python:3.13-slim` 元数据拉取失败）→ 退化 `docker cp` 注入新 `app.js`/`style.css` 到运行中的 `agentboard-web-1`（/app/agentboard/web/static/）。HTTP 校验 served app.js 含 `function showAuthScreen`、style.css 含 `.auth-card`；线上 API 实测 register→201+token、me(token)→200、me(无)→401、错误密码→401。**注意**：改动仅在运行容器，未烤进镜像；待 Docker Hub 可达后需 `docker compose up -d --build` 固化。
- **提交/推送**：commit `df20152`，`git push origin main` 成功（`181fa99..df20152`）。
- **遗留**：为验证在线上 MariaDB 注册了探针用户 `__epic7_v2__`（id=1）等，无删除端点，作开发实例脏数据留存；如需清理需直连 MariaDB。
- **下一个 pending 项**：Epic 8 MariaDB 独立 `.sql` 脚本 + 真实集成验证 / Epic 9 Playwright E2E / Epic 12.2~12.4（Sprint/附件/定时 Agent）。

## Epic 8 MariaDB 独立 schema.sql + 真实集成验证 — 完成并交付
- **背景**：自动化周期执行。上一个 pending 即 Epic 8（MariaDB 脚本 + 真实验证 + 集成测试）。关键发现：**沙箱内 MariaDB 11 容器实际在运行**（`agentboard-db-1`，健康，宿主机 `localhost:13306`，库 `agentboard`/`agentboard`，账号 `agentboard`/`agentboard`，root `rootpass-change-me`），且上一提交已把部署切到 MariaDB → 真实验证条件具备。
- **交付物**：
  - `scripts/mariadb/schema.sql`：建库（utf8mb4）+ 建用户授权 + 6 表 DDL（`projects/epics/stories/tasks/users/comments`），字段/类型/唯一约束/外键与 `models.py` + Alembic 迁移完全对齐；`tasks.source_spec_id`、`priority DEFAULT 'medium'`、`ix_comments_task_id` 索引。
  - `scripts/mariadb/README.md`：离线评审 + docker 对接用法（双路径：Alembic 或 schema.sql；应用 `init_db` 对预建表容错）。
  - `tests/test_mariadb_integration.py`：`skipif` 无 `AGENTBOARD_TEST_MARIAODB`；init_db + service 冒烟（CRUD + 状态机 + 搜索 + 评论 + spec 生成子任务 + 级联删除）。
- **验证**：① 把 `schema.sql` 的 CREATE TABLE 段（剥离 `--` 注释与 CREATE DATABASE/USER/GRANT/USE）应用到隔离验证库 `agentboard_verify`，6 表结构正确、外键齐全，验证库已清理；② 集成测试指向专用库 `agentboard_ci`（root 授权 `agentboard`@`%` 后），**2 passed**；③ 回归 `test_web_flow + test_backend_flow` = **6 passed**；④ Docker 三容器均 healthy，api `/api/meta`=200、web `/`=200。
- **踩坑**：测试删除 project 后访问过期实例属性（`.id`）触发 SQLAlchemy `ObjectDeletedError` → 改为先捕获整型主键、用全新 session 断言删除。另：`agentboard` 用户仅拥有 `agentboard` 库权限，CI 库需单独 GRANT。
- **提交/推送**：commit 待执行（`feat(db): Epic 8 MariaDB 独立 schema.sql + 真实集成验证`）。
- **下一个 pending 项**：Epic 9 Playwright E2E / Epic 12.2~12.4（Sprint/附件/定时 Agent）。
