# 2026-08-05 工作日志

## 自动开发恢复：Epic 78 Story 177 Executor daemon 常驻模式 → Task 979 in_review（达成）

- **背景**：上轮（8-04）中断于「daemon 单测卡死」。恢复后定位根因：测试 `test_daemon_idles_without_pending_until_stop_event` 漏传 `stop_event=stop` 参数 → run_daemon 走 else 分支 `time.sleep(5.0)` 无限循环（faulthandler 抓栈确认在 executor.py line 1187）。**实现本身无 bug，是测试漏参**。修复后 5 单测 13s 全绿。
- **选型**：Epic 78 全部 Story 已 in_review，验收缺口 = `--daemon` 常驻模式（验收原文「执行器 daemon 运行后能真正触发 Agent 并落 success/failed」）。MCP 新建 Story 177 + Task 979（highest）。
- **交付**（纯增量，零 REST/DB 契约变更）：
  - `agentboard/executor.py` 新增 `run_daemon()`：循环认领 id 升序 pending AgentRun → 交 execute_run（自动分派 Launcher/Trigger + 感知 report_run_result 回写）；无 pending 走 `stop_event.wait(idle_sleep)` 可唤醒；max_runs 限制；单 run 异常兜底 failed 不拖垮循环；返回 `{processed, last_status, stopped}`。
  - CLI `--daemon` / `--daemon-poll-interval` / `--daemon-idle-sleep` / `--daemon-max-runs`（max-runs=0 立即退出冒烟）。
- **验证**：
  - 单测 5 passed（逐个处理/max_runs/idle+stop_event/异常兜底/None 计数/CLI max-runs=0）。
  - E2E 1 passed：自起 API+Web → REST 造 project+schedule(codex)+pending run → CLI `--daemon --daemon-max-runs 1` 真实处理（run 离开 pending）→ Playwright 项目视图 0 报错/0 404。
  - 聚焦回归 100 passed/1 failed：failed 为 test_smoke 单独跑 8 passed，属既有「story 系模块级 env 污染」预存在模式，非本次回归。
- **坑**：① Windows Git Bash 无 `timeout`/`sleep` 命令（被解析为 Windows 同名词）→ 用 Python 子进程 `Popen`+轮询限时；② 调试 stdout 经 `tail` 管道会缓冲丢失 → 重定向到文件；③ `session_scope` 是 `@contextmanager`，`with session_factory() as s:` 合法。
- **状态**：Task 979 / Story 177 → in_review；Epic 78 现有 8 个 Story 全部 in_review。
- 提交 `3e9b81c`，push origin main 成功（d1b377b..3e9b81c）。未触碰 18001/docker。autodev.lock 已删。

## Story/Epic 评论功能（Epic 97 扩展）

- **需求**：Story、Epic 实体也要支持与 Task 一致的评论（列表/发表/Markdown 预览/@提及通知）。
- **实现**（六层同步）：
  - 模型 `domains/work_items/models.py`：`Comment` 增 `story_id`/`epic_id` 可空 FK+索引，`task_id` NOT NULL→可空（三者恰好其一）。
  - 迁移 `n1o2p3q4r5s6`：add_column+index；SQLite 下 task_id nullable 化须 `batch_alter_table`（SQLite 不支持 ALTER COLUMN），MariaDB 走 `op.alter_column`。
  - service：`_comment_target` 校验「三者恰好其一」；`create_comment`/`list_comments` 泛化为关键字参数（`task_id=`/`story_id=`/`epic_id=`）；`get_comment_project_id` 按实体分支上溯；`delete_epic`/`delete_story` 级联清评论。
  - api：新增 `GET/POST /api/stories/{sid}/comments`、`GET/POST /api/epics/{eid}/comments`；@提及逻辑抽 `_mention_notify` 共用（link=/story/{sid}、/epic/{eid}）；访问控制/审计天然覆盖（`_resolve_project_id_from_request` 的 story/epic 分支 + audit 实体表已存在）。
  - 前端：`Comment` 接口三 id 可空；api.service 4 新方法；app.html Epic/Story detail-panel 末尾嵌 `.comments-card`（复用 task 版样式，模板变量用 epAuthor/epContent/stAuthor/stContent 防冲突）；app.ts 增 `storyComments`/`epicComments` signal，navigate() 并行加载，`addStoryComment`/`addEpicComment` 发布后仅重载评论列表（**不用 `run()` 全量 refresh，避免重置 epicTab/storyTab**）。
  - MCP：`list_story_comments`/`add_story_comment`/`list_epic_comments`/`add_epic_comment`（18001 容器内存旧代码，重部署才生效，靠自包含测试验证）。
- **测试** `tests/test_epic97_story_epic_comments.py`（6 passed）：CRUD+字段正确+不串实体、404/422、@提及通知 link、REQUIRE_AUTH=1 下非成员 403/成员放行、级联删除、MCP 4 工具真调；`test_epic97_mcp_tool_availability.py` 5 passed 无回归。
- **前端构建**：npm run build → main-I47OP6GM.js + styles-UDA3APSZ.css → cp web/static；清理旧 hash 产物（git rm 5 个跟踪文件 + rm 4 个未跟踪残留 + 嵌套 static/static 目录）。Windows 下 `rm` 未跟踪文件被 safe-delete 拦（相对路径）→ 用绝对路径重试成功。
- 提交中，push 结果见下方。

