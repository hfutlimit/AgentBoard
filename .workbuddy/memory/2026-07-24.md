# 2026-07-24 工作日志

## 11:26 自动开发 — Epic 51 v3.8 任务列表行内快速指派（达成）
- 纯前端补齐「行内快速指派」，与 v3.4 行内状态切换对称；点击指派人头像弹成员浮层即时改派/取消（调 `api.updateTask(id,{assignee_id})`）。
- REST 追踪：project 114(ADV38) / epic 122 / story 192 / task 998(high) → 合法链 backlog→todo→in_progress→in_review；story/epic 同步 in_review。
- 验证：Playwright `tests/test_epic51_v38_inline_assign_e2e.py` 全绿（0 pageerror/console/.js+.css 404，含指派/取消 API 复核、遮罩关闭）；为造成员临时把 admin 加 project 3 成员、测试末还原。回归 pytest epic30_cache 7passed/1skip + E2E v3.4/v3.7 全绿。
- 坑：首版 Edit 误删指派人头像 `title` 属性（丢失 tooltip）→ 已还原。
- 构建 `npm run build`(node22.22.2) → cp `frontend/dist/frontend/browser/.` → `agentboard/web/static/`，删旧 `main-WXZPDYFU.js`、新 `main-DXSJYRMB.js`。
- 提交 `feat(ui): 前端小优化 - 任务列表行内快速指派 (Epic 51 v3.8)` + push origin main。
- 硬约束：未触碰 18001(MCP)/docker；排除 data/、autodev.lock、其他 automation memory.md、screenshots、前端 dist 源码。

## 14:36 自动开发 — Epic 52 v3.9 任务列表行内快速编辑截止日期（达成）
- 纯前端补齐「行内快速编辑截止日期」，完成行内交互家族三件套（状态 v3.4 / 指派 v3.8 / 截止日期 v3.9）；任务行截止日期徽章始终可点击（有日期显示日期、无日期显示「设截止」），点击弹 fixed 浮层（复用 `.status-menu` 定位）含原生 `type=date` 输入框 + 「应用」「清除」按钮，调 `api.updateTask(id,{due_date})` 即时更新并 `tasks.update` 局部刷新。
- REST 追踪（API 18000 / web 28080，admin id=54）：project 115(AUTODEV52) / epic 123(Epic 52 v3.9) / story 193 / task 999(high) → 合法链 backlog→todo→in_progress→in_review；story 193、epic 123 同步 in_review（本次「task → in_review」目标达成）。
- 实现：app.ts（dueMenuTaskId/dueMenuPos 信号 + dueMenuInitial 预填 + openDueMenu/closeDueMenu/quickSetDue，含 `(task.due_date||'').slice(0,10)` 同值早退）；app.html（due-pill 可点 + `.due-menu` 浮层含 `#dueInput` 模板引用 + 遮罩）；app.css（.due-pill / .due-menu-* 复用既有一致主题变量，含 dark）。零后端契约变更。
- 构建 `npm run build`(node22.22.2, NODE_OPTIONS=--max_old_space_size=4096, 清 .angular/cache) → cp `frontend/dist/frontend/browser/.` → `agentboard/web/static/`，删旧 `main-DXSJYRMB.js`/`styles-XKIGUZPX.css`、新 `main-65OMIUYJ.js`。
- 验证：Playwright `tests/test_epic52_v39_inline_due_e2e.py` 全绿 —— 登录 admin→/story/193→点 due-pill→浮层预填→设 2026-08-01（API 复核==2026-08-01 + UI 离开空态 has-due）→改 2026-08-15（复核==2026-08-15）→清（复核 null + UI 回「设截止」）→遮罩关闭；**0** pageerror/console/.js+.css 404；测试末 PATCH 还原 task 999 due_date=null（不改 backlog 状态）。回归 pytest epic30_cache 7passed/1skip + E2E v3.8/v3.7 全绿。
- 坑(已记): `formatDueDate` 渲染为 `MM/DD`(zh-CN) 而非 ISO → E2E UI 断言改以「离开设截止空态 + has-due class」判定，精确日期以 API 复核为准。
- 提交 `feat(ui): 前端小优化 - 任务列表行内快速编辑截止日期 (Epic 52 v3.9)` + push origin main。
- 硬约束：未触碰 18001(MCP)/docker；排除 data/、autodev.lock、其他 automation memory.md、screenshots、前端 dist 源码（仅提交 static 产物 + 源码 + 测试）。

## 2026-07-24 17:4x 自动开发 — Epic 53 v4.0 筛选预设增强（达成）
- 目标：本次 task → in_review。MCP 连接器全断 → REST 兜底（API 18000 / web 28080，admin 54）。
- 选型：v 系列（v1.5~v3.9）已齐；最高优先级 backlog（文档维护 Epic 15 / 腾讯云 COS Epic 64）依赖重、1 小时不可独立收尾 → 延续增量，做 v4.0「筛选预设增强 — 多命名预设 + 默认预设」。
- 追踪（REST 新建）：project 116(AUTODEV53)→epic 124(Epic 53 v4.0)→story 195→task 1006(high) → 合法链 backlog→todo→in_progress→in_review；story 195、epic 124 同步 in_review（达成）。
- 实现（纯前端，零后端契约变更）：
  - FilterPreset 接口加 id/isDefault + 全量多选数组（statuses/priorities/types/assignees）+ groupBy/sortKey/sortOrder；loadFilterPresets 兼容 v3.1 旧单值结构迁移。
  - saveFilterPreset 捕获全量状态；applyFilterPreset(id)/deleteFilterPreset(id) 改按 id；新增 setDefaultPreset(id)（同时仅一个默认）+ applyDefaultPreset() + defaultPreset computed。
  - app.html 预设面板列出命名预设 + 星标（默认）切换 + 「应用默认」按钮；app.css 补 .preset-apply-default/.preset-star/.preset-item.is-default。
- 关键发现：当前 UI 仅剩 status+assignee 两条 chip bar（priority/type/due chips 已不在 UI 渲染，grep 无 toggleFilterPriority 引用），且 status/assignee/due 均为单选 → 多选数组捕获在 UI 不可触发，仅为前向兼容。
- 验证：Playwright test_epic53_v40_presets_enhanced_e2e.py 全绿（保存多命名预设/设默认/应用默认/应用指定/刷新持久化/删除、0 错误）；受控 story 195（8 QA 任务 + task1006，确定性状态/指派人）规避 story 25 数据漂移（全 done/全未指派）。回归 test_epic43_filter_presets_e2e.py（迁 story195+API 18000 端口修复）全绿；pytest test_epic30_cache.py 7passed/1skip；test_epic52_v39_inline_due_e2e.py 全绿。零回归。
- 构建：npm run build(node22.22.2, 清 .angular/cache) → cp dist/frontend/browser/. → agentboard/web/static/，删旧 main-65OMIUYJ.js、新 main-D3SGJYRX.js（web 28080 直读挂载，即时生效）。
- 提交：feat(ui): 前端小优化 - 任务列表筛选预设增强（多命名预设+默认预设）(Epic 53 v4.0) → push origin main。
- 硬约束：未触碰 18001(MCP)/docker compose/端口；排除 data/、autodev.lock、其他 automation memory、screenshots、前端 dist 源码。

## 2026-07-24 19:37 用户手动触发 docker 部署（验证用）
- 指令：「部署docker 我来测试下」。仅对 api + web 执行 `docker compose up -d api web`，未碰 mcp(18001)/db。
- 结果：api 重建并 healthy；web 保持运行（static 经挂载实时生效，返回新 main-D3SGJYRX.js）；18001 仍由 mcp 占用、未被重启。
- 访问：Web http://localhost:28080，API http://localhost:18000。包含 Epic 53 v4.0 筛选预设增强。

## 2026-07-24 19:47 设计原型专家团 — AgentBoard Story 任务视图原型 + 创建 Story
- 触发：用户贴 AgentBoard Story 任务视图截图，要求设计原型 + 在 AgentBoard 创建 Story 供后续自动化实现。
- 流程（设计引擎 SOP）：TeamCreate `design-engine-agentboard-task-view` → discovery-analyst(需求摘要) → design-system-expert(选 Linear 蓝紫令牌，输出 C:/Users/jason/AgentBoard-prototype/DESIGN.md + brand-spec.md) → prototype-builder(生成 E:\Projects\WorkBuddy\AgentBoard\agentboard-task-view-prototype.html，28KB/712 行/零外部依赖) → critique-reviewer(PASS 20/25, P0=0) → 修正 2 项 P1（"三舒适"→三栏、空状态加「新建任务」按钮）→ export-specialist(导出 deliverables\agentboard-story-view.html)。
- 设计决策：选 Linear（蓝紫 DNA）定制，主色 #5B5BD6、Inter、8px 圆角、轻阴影，与 AgentBoard 现有品牌一致。
- MCP/Story：目标项目 AgentBoard id=117，Epic「架构设计」id=126；REST 兜底（MCP 连接全断）POST /api/epics/126/stories 创建 **Story id=199**[status=backlog]，描述含 10 区块实现规格 + Angular 单体 App 技术约束 + 验收标准，供 auto-dev 消费。
- 关键环境事实（重要）：本次 MCP 连接器全部 disconnected，AgentBoard MCP 不可用 → 一律走 REST（API 18000，admin id=54 token 可 login 取得）；本地 uvicorn 58125 未监听（连接失败），Docker API 18000 正常。
