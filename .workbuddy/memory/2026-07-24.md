# 2026-07-24 自动开发日志

## Epic 48 v3.5 批量状态变更状态机感知（task 946 → in_review，达成）
- **目标**：本次 task → in_review。MCP 连接器全断 → REST 兜底（API 18000 / web 28080）。
- **选型**：v 系列 bulk 五件套已齐（status/priority/assignee/due/delete），但批量状态面板仍遍历全部 6 状态（与 v3.4 行内切换的状态机感知不一致）→ 补齐「批量状态面板状态机感知」。
- **追踪（REST 新建）**：project 111(ADV35)→epic 119(Epic 48 v3.5)→story 186→task 946(high) → 合法链 `backlog→todo→in_progress→in_review`；story 186、epic 119 同步 **in_review**（达成）。
- **实现（纯前端，零后端契约变更）**：
  - `app.ts` 新增 `readonly bulkLegalStatuses = computed<string[]>`：对 `selectedTasks` 取各自 `statusTransitions[t.status]` 做**逐任务交集**（首个初始化，后续 `filter` 保留共同项），空选/无共同项返回 `[]`。复用 v3.4 的 `statusTransitions` 镜像。
  - `app.html` 批量状态面板改为 `@if (bulkLegalStatuses().length>0)` 遍历交集渲染按钮；`@else` 渲染 `.muted` 空态提示「所选任务状态无共同可流转目标（受状态机限制）」；取消按钮保留。
  - **交集而非并集**：并集会把部分任务的非法目标暴露出来（点选后后端 `set_status` 400 部分失败）；交集保证「点哪个按钮，所有选中任务都能合法流转」。
- **构建坑（已记）**：① `npm run build` 须 managed node 22.22.2 + 清 `.angular/cache`；② 产物 `frontend/dist/frontend/browser/` → cp 至 `agentboard/web/static/`（docker volume 挂载即时生效），删旧 `main-EXWGHMZD.js`、新 `main-JABFCBHD.js`；③ Playwright Python `locator.first` 是**属性**非方法（`page.locator(...).first.click()`，不是 `.first().click()`）。
- **验证**：Playwright `tests/test_epic48_v35_bulk_status_fsm_e2e.py` 全绿 —— 选 `todo+todo+in_progress`→仅 1 按钮「完成」；选 `backlog+todo`→0 按钮+空态提示；**0** pageerror/console/.js+.css 404；自建 4 任务测试末 DELETE 清理。
- **回归**：`pytest test_epic30_cache.py` 7 passed/1 skipped；E2E v3.4 行内状态切换全绿（无回归）。
- **提交**：`feat(ui): 前端小优化 - 批量状态变更状态机感知 (Epic 48 v3.5)` → push origin main。
- **硬约束**：未触碰 18001(MCP)/docker compose/端口；web 28080 仍读 `agentboard/web/static` 挂载。
- **下次可执行**：v 系列 bulk 五件套 + 状态机感知已齐；可转向「筛选预设增强（默认预设/持久化应用）」「批量指派优化」或新需求。

## Epic 49 v3.6 任务列表分组新增「按优先级」（task 969 → in_review，达成）
- **目标**：本次 task → in_review。MCP 连接器全断 → REST 兜底（API 18000 / web 28080，admin id=54）。
- **选型**：分组维度已有 none/status/type/assignee + 折叠持久化（v1.8/v1.9），缺「按优先级」→ 补齐第 5 个分组维度（纯前端，零契约变更），与已有「优先级 chips(v2.0)/批量改优先级(v2.9)」信息体系一致。
- **追踪（REST 新建）**：project 112(AUTODEV49)→epic 120(Epic 49 v3.6)→story 187→task 969(high) → 合法链 `backlog→todo→in_progress→in_review`；story 187、epic 120 同步 **in_review**（达成）。
- **实现（纯前端）**：
  - `app.ts`：`taskGroupBy` 联合类型加 `'priority'`；`taskGroupOptions` 加 `{key:'priority',label:'按优先级'}`；`groupedTasks` 分桶加 `g==='priority' ? (t.priority||'medium')` 且键序用 `this.priorities`（highest→lowest）过滤；`groupLabel` 加 `mode==='priority'` 分支复用 `priorityLabel()`。
  - `app.html`：分组头在 `.task-group-label` 后新增 `@if(taskGroupBy()==='priority')` 的 `<span class="badge priority priority--{{grp.key}}">` 色徽章，复用既有 `.priority--*` 样式，无新增 CSS。
- **构建坑（已记）**：① `npm run build` 须 managed node 22.22.2 + 清 `.angular/cache`；② 产物 `frontend/dist/frontend/browser/` → cp 至 `agentboard/web/static/`，删旧 `main-JABFCBHD.js`、新 `main-OG767NBY.js`；③ **curl POST 带 `Authorization: Bearer` 头在 18000 上偶发「error parsing the body」**——改用 Python `urllib` 经 REST 新建/改状态全部 201/200（审计/访问中间件消费 body 的边界，urllib 稳定）；④ 校验编译产物用 Python 读 `main-*.js` 搜 `\u6309\u4F18\u5148\u7EA7`（esbuild 大写十六进制转义）确认模板进包。
- **验证**：Playwright `tests/test_epic49_v36_priority_group_e2e.py` 全绿 —— story 50（6 任务 high/medium/low 各 2）→ 选「按优先级」→ 3 组、DOM 顺序 high→medium→low、各组带 `priority--{x}` 徽章（文案 高/中/低）、计数和 2+2+2==6、刷新无关、**0** pageerror/console/.js+.css 404。
- **回归**：`pytest test_epic30_cache.py` 7 passed/1 skipped；E2E v3.5 批量状态机 / v3.4 行内状态切换 全绿（无回归）。
- **提交**：`feat(ui): 前端小优化 - 任务列表分组新增按优先级维度 (Epic 49 v3.6)` → push origin main。
- **硬约束**：未触碰 18001(MCP)/docker compose/端口；web 28080 仍读 `agentboard/web/static` 挂载；刻意排除 data/、autodev.lock、其他 automation 的 memory.md、screenshots、前端 dist 源码（仅提交 static 产物）、scratch 脚本（_ab_*.py/_ab_*.png/_ab_track49_ids.txt）。
- **下次可执行**：分组 5 维齐；可转向「筛选预设增强（默认预设/持久化应用）」「批量指派优化」「分组维度记忆（各视图独立）」或新需求。
