# 2026-07-28 自动开发日志

## 07:08 运行（Epic 70 v5.7 命令面板接入 Story/文档后端搜索 → in_review，达成）
- 目标：本次 task → in_review。MCP 连接器全断 → REST 兜底（API 58125 权威 / web 8090 验证，admin id=18）。
- 选型：v1.5~v5.6 已齐；依 07-28 03:51「下次可执行」建议（命令面板接入 Story/文档后端搜索）新建增量 Epic 70 v5.7，补齐命令面板第 3/4 类实体搜索。
- 实现：
  - 后端 `agentboard/service.py` 新增 `search_stories(s,q,limit)`；`api.py` 新增 `GET /api/search/stories`（路径避开与 `/api/stories/{sid}` 冲突；文档搜索复用既有 `/api/documents?q=`）。
  - 前端 `api.service.ts` 新增 `searchStories()`（30s TTL 缓存）；`app.ts`：`PaletteCommand.category` 扩展 `'story'|'document'`，新增 `paletteStoryResults`/`paletteDocumentResults` 信号并在 `openPalette`/`closePalette` 重置，`paletteRunSearch()` 并行拉取 Story+文档，`paletteItems` 合并四类结果；`app.html` 分类标签 ternary 扩展 + placeholder 更新；`styles.css` 新增 `.cat-story`(青)/`.cat-document`(橙)。
  - 关键坑：`/api/stories/search` 被 `/api/stories/{sid}` 路由吞掉 → 改名 `/api/search/stories`；`listDocuments` 不接受 `limit` 参数 → 去掉。
- 部署：本地 uvicorn 58125 重启（加载新端点）+ docker `agentboard-api-1` restart（`./agentboard` 只读 bind-mount，无需 cp）；前端 `npm run build`(node22.22.2, 清 .angular/cache) → `main-J3WWIUIZ.js`+`styles-O6FQPGRB.css` cp→`agentboard/web/static/`，删旧 `main-HUVLU7XG.js`/`styles-DI6XLDVB.css`。
- 验证：`tests/test_epic70_v57_palette_story_doc_e2e.py` ALL PASS（Story 1 条/文档 1 条结果、点击跳转 `/story/{id}`+`/documents/{id}`、空态、0 pageerror/console/.js+.css 404）；回归 `pytest test_epic30_cache.py` 8 passed + `test_epic69_v56_palette_search_e2e.py` + `test_epic67_v54_command_palette_e2e.py` ALL PASS（无回归）。
- 状态（REST 新建）：project 59(AUTODEV70)→epic 60(Epic 70 v5.7)→story 109(Story 70.1)→task 1125(high) → 经 `backlog→todo→in_progress→in_review` 合法链置 **in_review**；story 109、epic 60 经 PATCH 同步 **in_review**（达成）。本地 SQLite(58125) 权威库已落库。
- 提交：`feat(ui): 前端小优化 - 命令面板接入 Story/文档后端搜索 (Epic 70 v5.7)` → push origin main。刻意排除 data/、autodev.lock、其它 automation 的 memory.md、screenshots、frontend/dist、agentboard-audit/、其它运行遗留文件。
- 硬约束：未触碰 18001(MCP)/docker 端口映射；API 58125 / web 8090/8080 / docker 18000/28080 原样。
- 下次可执行：命令面板结果可加分组/图标优化，或新需求。

## 2026-07-28 10:19 自动开发 — Epic 71 v5.8 任务列表看板视图渲染（达成，task 1126 → in_review）

- **背景**：本次目标 task → in_review。MCP 全断，REST 兜底（API 58125 / web 8090，admin id=18）。
- **发现**：看板基础设施（`boardMode`/`setBoardMode`/看板 CSS/拖拽处理器/`tasksForStatus`/`toggleColumnCollapse`/`openQuickView`）早已落地，但 `app.html` 仅 `@if (!boardMode())` 列表分支、**缺 `@else` 看板渲染分支**，工具栏无切换入口、`v` 键提示未接线 → 看板从未真正可用。补齐最后一公里。
- **实现（纯前端，零后端契约变更）**：
  - `app.html`：列表区改 `@if/@else`，新增 `@else` 看板分支（7 列按 statuses 分桶；列头状态色点+名称+`getStatusTaskCount` 计数+`toggleColumnCollapse` 折叠；`.kanban-col-body` 拖拽目标；`.kanban-card` 可拖拽+点击 `openQuickView`+`toggleTaskComplete` 角标+优先级色边框+类型图标+`taskEpicName`+指派人头像+截止日期+`taskProgressPct` 进度，全复用既有）。`#densityToggle` 后新增 `#boardToggle`（列表/看板切换）。
  - `app.ts`：`handleTaskKeydown` 加 `case 'v'` 切 `boardMode`。
  - `app.css`：补看板基础布局（含 dark、列折叠态），复用既有优先级/进度/角标样式。
- **构建**：`npm run build`(node22.22.2, 清 `.angular/cache`, max_old_space=4096) → `main-MSF7W5XL.js`+`styles-O6FQPGRB.css` cp→`agentboard/web/static/`，删旧 `main-J3WWIUIZ.js`。app.css 经 ViewEncapsulation.None 注入主 JS（styles hash 未变正常）。
- **验证**：`tests/test_epic71_v58_board_view_e2e.py` 全绿（7 列/各状态列卡片+计数/点击打开抽屉+Esc/拖拽 backlog→todo API 复核 status=todo/切回列表/0 错误）；**关键发现：backlog→done 被状态机合法拒绝（非 bug）**。回归 `pytest test_epic30_cache.py` 8 passed + E2E v5.7/v5.6 全绿。
- **追踪（REST 新建）**：project 60(AUTODEV71)→epic 61(Epic 71 v5.8)→story 110→task 1126(high) → 合法链 `backlog→todo→in_progress→in_review`；story 110、epic 61 PATCH 同步 in_review。
- **提交**：`feat(ui): 前端体验升级 v5.8 - 任务列表看板视图渲染 (Epic 71, Task 1126 -> in_review)` → push `29263a7..ba1c5e7`。
- **硬约束**：未触碰 18001(MCP)/docker 端口；排除 data/、autodev.lock、其它 automation memory、screenshots、前端 dist、agentboard-audit/。

## 2026-07-28 13:53 自动开发 — Epic 72 v5.9 看板视图批量操作（卡片多选 + 复用批量工具栏，达成，task 1127 → in_review）

- **背景**：本次目标 task → in_review。MCP 全断，REST 兜底（API 58125 / web 8090，admin id=18）。真实 backlog 顶部仍为 junk（1112-1116 重复空描述 / 1107 __PROBE_500__）；high 项 1123-1126 已全部 in_review。依 v5.8 运行「下次可执行」建议（看板视图下批量操作）新建增量 Epic 72 v5.9。
- **选型发现**：经核查 `app.ts` 与 `app.html`，`bulk-action-bar` 位于 `@if (!boardMode())` 分支**之外**（列表/看板共用），随 `selectedTasks().size>0` 自动出现；`toggleTaskSelection`/`bulkUpdate*`/`bulkLegalStatuses` 全部就绪；看板 `tasksForStatus` 已基于 `visibleTasks()` 故筛选联动天然支持。缺口仅在于**看板卡片无选择入口**。
- **实现（纯前端，零后端契约变更，<10 行模板+CSS）**：
  - `app.html`：看板 `<article>` 加 `[class.selected]="selectedTasks().has(t.id)"`；`.kanban-card-top` 首部新增 `.kanban-card-check` 复选框，绑定 `toggleTaskSelection(t.id)`，并 `(mousedown)`+`(click)` 双 `stopPropagation`（防误开快速查看抽屉 / 防 draggable 父元素发起拖拽）。
  - `app.css`：新增 `.kanban-card-check` + `.kanban-card.selected`（含 `[data-theme=dark]`），复用品牌色变量。app.ts 无改动。
- **构建**：`npm run build`(node22.22.2, 清 `.angular/cache`, max_old_space=4096) → `main-GS2YFXXM.js`（styles hash 不变 O6FQPGRB，组件 CSS 注入主包）cp→`agentboard/web/static/`，删旧 `main-MSF7W5XL.js`。
- **验证**：`tests/test_epic72_v59_board_bulk_select_e2e.py` ALL PASS（种子 story+3 任务 → 看板勾选卡片→`.selected`+不触发抽屉+共享批量工具栏「2 项已选」→状态机感知批量改状态 B/C 均 done（API 复核）→Esc 清除→切回列表→**0** pageerror/console/.js+.css 404）。回归 `pytest test_epic30_cache.py` 8 passed + `test_epic71_v58_board_view_e2e.py` ALL PASS（无回归）。`test_epic55_v42_quick_view_drawer_e2e.py` 因硬编码 28080（已宕机，curl→000）失败，属预先存在端口漂移，非本次回归（卡片点开抽屉路径已由 v5.8 E2E 在 8090 覆盖且通过）。
- **追踪（REST 新建）**：project 61(AUTODEV72)→epic 62(Epic 72 v5.9)→story 111(Story 72.1)→task 1127(high) → 合法链 `backlog→todo→in_progress→in_review`；story 111、epic 62 PATCH 同步 **in_review**（达成）。
- **提交**：`feat(ui): 前端体验升级 v5.9 - 看板视图批量操作（卡片多选 + 复用批量工具栏）(Epic 72, Task 1127 -> in_review)` → push `ba1c5e7..580bed7`（8 文件 +274/-6）。刻意排除 data/、autodev.lock、其它 automation memory、screenshots、前端 dist、agentboard-audit/、scratch 脚本。
- **硬约束**：未触碰 18001(MCP)/docker 端口映射；API 58125 / web 8090/8080 / docker 18000/28080 原样。
- **下次可执行**：看板列按分组维度（优先级/负责人）分列、看板筛选预设联动可视化、或新需求。

## 2026-07-28 17:0x 自动开发 — Epic 73 v6.0 看板视图列全折叠/全展开 → in_review（达成）
- **目标**：本次 task → in_review。MCP 连接器全断 → REST 兜底（API 58125 权威 / web 8090 验证，admin id=18）。
- **选型**：真实 backlog 顶部为 junk（1112-1116 重复空描述），high 项 1123-1127 全 in_review；依 v5.9「下次可执行」建议补齐看板对称能力 —— 列表 v1.9 已有分组一键全折叠/全展开，看板仅 v5.8 单列折叠 → 新建增量 Epic 73 v6.0「看板视图列全折叠/全展开」。
- **追踪（REST 新建）**：project 62(AUTODEV73)→epic 63(Epic 73 v6.0)→story 112(Story 73.1)→task 1128(high) → 合法链 `backlog→todo→in_progress→in_review`；story 112、epic 63 经 PATCH 同步 **in_review**（达成）。
- **实现（纯前端，零后端契约变更）**：`app.ts` 新增 `allColumnsCollapsed` computed + `collapseAllColumns()`（将 7 状态全部加入 `collapsedColumns` 信号并写 `localStorage.agentboard_collapsed_cols`）/ `expandAllColumns()`（清空）；`app.html` 工具栏 `#boardToggle` 后新增 `@if (boardMode())` 包裹的 `#boardColsToggle` 切换按钮（文案 全折叠↔全展开）；`app.css` 新增 `.btn.board-cols-toggle`（镜像 `.density-toggle`）。构建 `main-A6KMV6OY.js` cp→`agentboard/web/static/`，删旧 `main-GS2YFXXM.js`。`openspec/changes/board-collapse-all-v60/{proposal,design,tasks}.md` 已写。
- **验证**：`tests/test_epic73_v60_board_collapse_e2e.py` 全绿 —— 看板 7 列渲染/按钮仅看板可见/点「全折叠」→7 列全 `.collapsed`+文案切「全展开」/点「全展开」→0 折叠+文案回「全折叠」/刷新后折叠持久化/**0** pageerror·console·.js+.css 404。回归 `pytest test_epic30_cache.py` 8 passed + `test_epic71_v58_board_view_e2e.py`(v5.8) ALL PASS + `test_epic72_v59_board_bulk_select_e2e.py`(v5.9) ALL PASS（无回归）。
- **提交**：`feat(ui): 前端体验升级 v6.0 - 看板视图列全折叠/全展开 (Epic 73, Task 1128 -> in_review)` → push origin main。刻意排除 data/、autodev.lock、其它 automation memory、screenshots、前端 dist 源码（仅提交 static 产物）、agentboard-audit/、scratch 脚本。
- **硬约束**：未触碰 18001(MCP)/docker 端口；API 58125 / web 8090/8080 / docker 18000/28080 原样。
- **下次可执行**：看板列按分组维度（优先级/负责人）分列、看板筛选预设联动可视化、或新需求。
