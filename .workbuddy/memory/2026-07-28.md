# 2026-07-28 工作日志

## 20:xx 自动开发 — Epic 74 v6.1 看板视图列内按维度子分组 → in_review（达成）
- **目标**：本次 task → in_review。MCP 连接器全断 → REST 兜底（API 58125 权威 / web 8090 验证，admin id=18）。
- **选型**：真实 backlog 顶部仅剩 junk（#1112-1116 重复空描述 Playwright 探针）；high 项 #1123-1128 全 in_review；`filterMineOnly`(我的任务)/`boardMode` 持久化等均已落地 → 依 v5.9 提示「看板列分组维度分列」新建增量 Epic 74 v6.1「看板视图列内按维度子分组」。
- **追踪（REST 新建）**：project 63(AUTODEV74)→epic 64(Epic 74 v6.1)→story 113(Story 74.1)→task 1136(high) → 合法链 `backlog→todo→in_progress→in_review`；story 113、epic 64 经 PATCH 同步 **in_review**（达成）。
- **实现（纯前端，零后端契约变更）**：
  - `app.ts` 新增 `boardSubGroups(status)`：复用 `taskGroupBy`/`groupLabel`/`dueBucket`/`priorities` 分桶逻辑（与列表 `groupedTasks` 完全一致）；`taskGroupBy='none'|'status'` 返回单平铺子分组；空列返回 `[]`。
  - `app.html` 看板 `.kanban-col-body` 由平铺 `@for (t of tasksForStatus(s))` 改为 `@for (grp of boardSubGroups(s))`：有 `grp.key` 时渲染 `.kanban-subgroup-header`（标签+计数），卡片移入 `.kanban-subgroup-body`；`@empty` 保留「暂无任务」。拖拽目标 `.kanban-col-body` 与状态主轴不变。
  - `styles.css` 补 `.kanban-subgroup*`（sticky 子分组头 + 胶囊计数，基于 `--surface-2/--border/--brand-*` 主题变量，含 `[data-theme="dark"]`）。
- **构建**：`npm run build`(node22.22.2, 清 `.angular/cache`, NODE_OPTIONS=--max_old_space_size=4096) → `main-FOZ5QS6F.js`+`styles-W5HB4YXQ.css` cp→`agentboard/web/static/`，删旧 `main-A6KMV6OY.js`。web 8090 直读挂载即时生效。
- **验证**：`tests/test_epic74_v61_board_subgroup_e2e.py` 全绿 —— 默认不分组=平铺(1 无头子分组/3 卡片)；按优先级=3 子分组头(高/中/低, 计数[1,1,1])+卡片归位+拖拽目标仍在；按类型=3 头(Task/Bug/Test Execution)；切回不分组退化正确；**0** pageerror/console/.js+.css 404；测试末清理种子 story 114+任务，不污染追踪实体。回归 `pytest test_epic30_cache.py` 8 passed + E2E v5.8(拖拽)/v5.9(批量)/v6.0(折叠) 全绿（无回归）。
- **提交**：`feat(ui): 前端体验升级 v6.1 - 看板视图列内按维度子分组 (Epic 74, Task 1136 -> in_review)` → push origin main。刻意排除 data/、autodev.lock、其它 automation 的 memory.md、其它日期的 daily memory、screenshots、agentboard-audit/、deliverables/、docs/design-prototypes/、其它 scratch 脚本。
- **硬约束**：未触碰 18001(MCP)/docker 端口；API 58125 / web 8090/8080 原样。
- **下次可执行**：看板子分组头折叠/展开、看板视图下筛选预设联动可视化、或新需求。

## 21:xx 修复 MCP `list_projects` 越权查看全部项目缺陷
- **现象**：MCP 连接器（mcp.json → `http://124.220.44.12/mcp`）`list_projects` 返回全部项目（含非成员项目）。根因：MCP 用 `abk_` 管理员 API Key（或管理员用户令牌）调 `/api/projects`，而 `list_accessible_projects` 对 admin 返回全量。
- **修复（3 文件，已 push `5b1f227`）**：
  1. `agentboard/mcp_server.py` `_proj_list`：改调成员作用域端点 `/api/users/me/projects`（纯成员 JOIN，永不 admin 全量），`list_projects` docstring 同步。
  2. `agentboard/api.py` `list_projects_ext`：检测 `abk_` API Key 调用 → `service.list_accessible_projects(include_admin=not is_api_key)`；交互式用户令牌仍保留 admin 全量视图（web 不回归）。
  3. `agentboard/service.py` `list_accessible_projects`：新增 `include_admin: bool = True` 参数，API Key 调用强制成员作用域。
- **部署关键点**：`124.220.44.12/mcp` 是**独立远程生产部署**（非本机 docker；本机 `agentboard-mcp-1`@18001 日志无连接器调用，SSH 22 拒绝，git remote 仅 GitHub）。修复需在生产重新部署（pull GitHub 或 rebuild 镜像）。本机 docker API(18000)/MCP(18001) 已 live 修复验证。
- **本地验证（in-container fastmcp client 打 8002 修复实例）**：管理员令牌 `list_projects` BEFORE=[]（修复前会全量 75）；`create_project`(125) 后 `list_projects`=仅 125（membership_role=owner，非全量）；`create_epic`(132)→`create_story`(204, backlog)→`update_story(status=done)`=完成。验证后清理 demo 数据（project125 级联 + 测试用户 73）。
- **结论**：MCP 现在只返回「自己创建 / 作为成员」的项目；创建+完成 story 流程正常。

## 21:4x 将 MCP apikey 权限改为与关联用户一致（回退 hack + 重写 make-mcp-token.py）
- **用户要求**："mcp 的 apikey 关联到用户，权限和用户是一致的，不应该一直有管理员权限"。
- **分析**：API Key 解析（`_current_user` / `_caller_uid_admin`）已正确读取关联用户的 `is_admin`，无自动提权。根因是 `make-mcp-token.py` 硬编码为**第一个管理员**创建 key → 管理员身份泄漏。上轮 `include_admin=False` hack 强制 API Key 为非管理员，违背"权限与用户一致"原则。
- **回退（api.py + service.py）**：移除 `include_admin` 参数和 `is_api_key` 检测，恢复 `list_accessible_projects` 的原始 `user.is_admin` 控制逻辑。API Key 权限 = 关联用户的完整身份（含 is_admin）。
- **重写 make-mcp-token.py**：新增 `--user` 参数指定已有用户；默认创建**非管理员** `mcp-service` 用户并为其生成 key；如果 `--user` 指定的是管理员则打印 WARNING。权限统一 `api:*`。
- **防御保留**：`mcp_server.py` 的 `_proj_list` 仍调 `/api/users/me/projects`（成员作用域端点）—— 即使 key 未来被设为管理员，列表也仅成员。
- **本地验证**：非管理员 `abk_` key：`/api/projects` → total=0（无成员时空）→ create project → total=1 → create epic→story→complete(backlog→done) 全绿；`/api/admin/projects` 403；admin 用户令牌 `/api/projects` total=77（Web 不受影响）。
- **提交** `11a5b35`：`fix: MCP apikey 权限与关联用户一致，杜绝默认管理员权限`；仅 3 文件。

## 23:xx 自动开发 — Epic 75 v6.2 看板视图子分组头折叠/展开 → in_review（达成）
- **目标**：本次 task → in_review。MCP 连接器全断 → REST 兜底（API 58125 权威 / web 8090 验证，admin id=18）。
- **选型**：真实 backlog 顶部仍为 junk（#1112-1116 `Playwright验证-文档Story路径`）+ 测试种子（AT/BT-unique-*、seed board task）；唯一真实 in_progress 为 #708（medium，性能指标显示，scope 模糊）；依 v6.1「下次可执行」建议新建增量 Epic 75 v6.2「看板视图子分组头折叠/展开」，纯前端、零后端契约变更。
- **追踪（REST 新建）**：project 64(AUTODEV75)→epic 65(Epic 75 v6.2)→story 114(Story 75.1)→task 1137(high) → 合法链 `backlog→todo→in_progress→in_review`；story 114、epic 65 经 PATCH 同步 **in_review**（达成）。
- **实现（纯前端）**：
  - `app.ts`：新增 `collapsedSubgroups` 信号（key=`status::subgroupKey`，localStorage `agentboard_collapsed_subgroups`）+ `isSubgroupCollapsed`/`toggleSubgroupCollapse`/`hasSubgroups`/`allSubgroupsCollapsed`/`collapseAllSubgroups`/`expandAllSubgroups`（镜像 v6.0 列折叠模式，`boardSubGroups` 复用 v6.1）。
  - `app.html`：子分组头 `.kanban-subgroup-header` 改为可点击（chevron ▾/▸ + `[class.collapsed]`）；列头在 `hasSubgroups(s)` 时新增 `.kanban-col-subgroups-toggle` 按钮（按列一键折叠/展开全部子分组，stopPropagation 防误触整列折叠）；`.kanban-subgroup-body` 包入 `@if(!grp.key || !isSubgroupCollapsed(...))`。
  - `styles.css`：`.kanban-subgroup-header` 可点/hover/collapsed 态 + `.kanban-subgroup-chevron` + `.kanban-col-subgroups-toggle`（含 dark）。
- **坑（已解决）**：`boardSubGroups(status: Status)` 形参为 `Status` 联合类型 → 我的 helper 初始写 `status: string` 触发 TS2345；改为 `Status` 后构建通过。
- **构建**：`npm run build`(node22.22.2, 清 `.angular/cache`, NODE_OPTIONS=--max_old_space_size=4096) → `main-YJGOCOWE.js`+`styles-DRM33VRW.css` cp→`agentboard/web/static/`，删旧 `main-FOZ5QS6F.js`/`styles-W5HB4YXQ.css`；web 8090/8080 直读挂载即时生效（均确认服务新包）。
- **验证**：`tests/test_epic75_v62_board_subgroup_collapse_e2e.py` 全绿（7 列渲染 / 优先级 3 子分组头计数[1,1,1] / 列头折叠按钮存在+拖拽主轴仍在 / 单子分组折叠(collapsed+body 隐藏+卡片-1) / 再次点击展开 / 列头全部折叠(0 body 0 卡片)→全部展开 / reload 后持久化 / **0** pageerror·console·.js+.css 404；测试末清理种子）。回归 `pytest test_epic30_cache.py` 8 passed + `test_epic74_v61_board_subgroup_e2e.py` ALL PASS（无回归）。
- **提交**：`feat(ui): 前端体验升级 v6.2 - 看板视图子分组头折叠/展开 (Epic 75, Task 1137 -> in_review)` → push origin main。刻意排除 data/、autodev.lock、其它 automation 的 memory.md、screenshots、前端 dist、agentboard-audit/、scratch 脚本（ab_create_epic75.py 在 C:/Users/jason 不在仓库）。
- **硬约束**：未触碰 18001(MCP)/docker 端口；API 58125 / web 8090/8080 / docker 18000/28080 原样。
- **下次可执行**：看板视图下筛选预设联动可视化、或新需求。
