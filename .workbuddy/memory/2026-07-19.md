# 2026-07-19 自动开发记录

## Epic 29 (v1.8): 可折叠任务分组 → done ✅

### 完成内容
- **Epic 29 / Story 65 / Task 837** 全部 done
- 功能：任务列表分组后（按状态/类型/负责人），组标题可点击折叠/展开
- 实现：
  - `collapsedGroups` signal（`Set<string>`），从 localStorage `agentboard_collapsed_groups` 初始化
  - `isGroupCollapsed(key)` / `toggleGroup(key)` 方法
  - HTML：组标题加 `.task-group-chevron`（▸/▾）+ `(click)` + `[class.collapsed]`
  - `@if (!grp.key || !isGroupCollapsed(grp.key))` 包裹组内 `@for` 控制可见性
  - CSS：`.task-group-header` cursor pointer + hover + chevron 旋转过渡
  - angular.json anyComponentStyle budget 36kB→40kB（CSS 超 236 bytes）
- 净增 ~30 行（app.ts+10 / app.html+4 / app.css+4），符合 R2
- commit `3e39c2e`，push 成功

### 验证
- Playwright E2E（`tests/test_v18_collapsible_groups_e2e.py`）：
  - 2 个分组标题出现（按状态分组）
  - 折叠：156→154 items（backlog 组 2 项隐藏）
  - 展开：156 items 恢复
  - chevron 图标存在
  - localStorage 持久化 `["backlog"]`
  - 不分组模式无标题
  - **零 page/console/404 错误**

### 回归测试
- pytest 核心测试运行中（test_backend_flow / test_crud_smoke / test_api_keys / test_labels_api / test_due_date）
- MCP 测试因 `fastmcp` 未安装跳过（既有问题）

### 注意事项
- 本地 API (58125) 未运行，需手动启动 `uvicorn agentboard.api:app --port 58125`
- MCP auth 仍 unauthorized，继续使用 REST API 更新状态
- Task 837 状态迁移：backlog→todo→done（经 A-22 例外允许 todo→done）

## 新增 Epic：项目文档维护（多成员/多 Agent 协作）→ Epic id=15

经 AgentBoard MCP（project_id=3）创建，需求源自 https://github.com/hfutlimit/AgentBoard。
- **目标**：新增独立 Document 实体，支撑多成员/多 Agent 协作——互相 review plan、共同维护 memory、沉淀 knowledge/design；文档独立于 Task.spec。
- **文档类型**：memory / plan / knowledge / design；**状态机**：draft → in-review → approved/cancelled；内容 Markdown + Mermaid 渲染；支持评论增/改；MCP 暴露文档管理 API。
- **9 个 Story**（均 backlog）：
  - S1(id=46) 数据模型与迁移 Document+DocumentComment（models.py + Alembic，SQLite/MariaDB 兼容）
  - S2(id=52) 文档 CRUD REST API（api.py+service.py，DocumentIn/Patch/Out）
  - S3(id=53) 文档评审状态流转工作流（service.py TRANSITIONS，PUT /status）
  - S4(id=47) 文档评论 API（POST/GET/PATCH comments）
  - S5(id=45) 前端文档列表与 Markdown 编辑器
  - S6(id=49) 前端 Markdown + Mermaid 渲染
  - S7(id=48) 前端文档评论区 UI
  - S8(id=51) MCP 文档管理工具集（用 _http，避开 _api 未定义缺陷）
  - S9(id=50) 多 Agent 协作与 memory 共同维护闭环（端到端 + OpenSpec 文档）
- **MCP 经验补充**：本轮 create_epic/create_story 均成功（auth 可用）；create_story 偶发 "root: must be object" 序列化错误（S2/S3 首次失败），原样重试即成功——属沙箱间歇性抖动，非参数问题。

## 全量迁移 tasks.md → MCP + COS 图片 Epic（22:55 起）

### 关键发现：存在 4 个分裂的数据库
- **远程 MCP（权威）= `http://124.220.44.12/mcp`**（WorkBuddy mcp.json 配置，Bearer `abk_...`）。上一轮建的文档 Epic(15)+9 Story 在此。
- 本地 `root agentboard.db`(58125/8000) → 24 epic，不含文档 Epic。
- `18000` Docker API(MariaDB) → 15 epic（ids 1-11,69,89-91），不含文档 Epic。
- `agentboard_data/agentboard.db` → 另一份。
- **结论**：迁移必须走 MCP 工具（唯一与远程权威一致路径）。远程 REST 直连需 `api:*` token，但 make-mcp-token.py 只对本库生成、且当前 abk_ token 缺 `api:read`，故放弃脚本直连，全程用 MCP 工具。

### 迁移执行（全量，含去重）
- 解析 tasks.md：27 Epic / 76 Story / 250 Task（203 done + 40 todo）。
- 抽查确认：远程 Epic 1 已有 Story 1.1/1.2(done)，即本地 40 个 todo 中 **38 个均属远程已 done 重复项**；真正缺失仅 **Epic 30（前端体验升级 v0.8）+ Task 801/802**。
- 去重逻辑：`[x]`→删本地；`[ ]`→若在远程有对应项则仅删本地（不重建），仅真正缺失项建 Story。
- 远程新建：Epic 30(id=63) + Story 59/60（Task 801/802）。
- 清理 tasks.md：删除全部 250 条任务行（0 剩余），保留 27 Epic/76 Story 骨架 + 顶部"以 MCP 为唯一真相源"说明。备份 tasks.md.bak 已删（原内容在 git 历史可恢复）。
- **git push 待执行**（SSH over 443：ssh://git@ssh.github.com:443/hfutlimit/AgentBoard.git）。

### 新增 Epic：腾讯云 COS 图片上传（id=64）
- 目标：COS 上传后端（配置占位 COS_SECRET_ID/KEY/BUCKET/REGION，密钥后续配，未配时优雅降级）+ 文档/评论/Story·Epic 描述均支持图片展示。
- 4 个 Story：S1(61) COS 上传后端、S2(62) 文档图片、S3(63) 评论图片、S4(64) Story/Epic 描述图片。均 backlog。
- 注意：文档图片依赖 Epic 15 的 Document 实体（S2 引用 S6 渲染管线）；密钥未配不阻断。
