# 2026-07-27

## admin-portal 基础框架落地（Task 850/851/856 → in_review）
- 在 `frontend/` Angular 21 工作区用 `ng generate application admin-portal` 新增第二个应用，复用已有 node_modules，无需 npm install。
- 实现：登录页（调 `/api/auth/login`、存 token、跳 dashboard）、函数式路由守卫、受保护 dashboard 占位、全局 premium 主题（light/dark）。
- **关键坑**：`/api/auth/login` 返回字段是 `token`（不是 `access_token`）；初版误用 `access_token` 导致 `me()` 401。已修正。
- 验证：`ng build admin-portal` 通过；`tests/test_admin_portal_login_e2e.py` 全绿（0 pageerror/console/.js+.css 404，无预期外 401）；`pytest test_epic30_cache.py` 8 passed；主 `frontend` 构建无回归。
- dev server：`ng serve admin-portal --host 127.0.0.1 --port 4300 --proxy-config projects/admin-portal/proxy.conf.json`（注意须 `--host 127.0.0.1`，默认 localhost 仅绑 IPv6 `::1`，Playwright 用 127.0.0.1 连不上）。
- 状态：Task 850/851/856 经 REST 合法链 `backlog→todo→in_progress→in_review` 置 in_review；Story 71 保持部分完成。
- 端口纪律：未触碰 18001(MCP)/docker；API 58125、web 8080/8090/28080 原样。

## 统计页落地（Task 854 实现统计页 → in_review）
- **选型**：admin-portal 最高优先级真实 backlog；Story 71 中 Task 850/851/852/853/855/856 已 in_review，仅 Task 854（实现统计页）仍 backlog → 本次独立交付。
- **实现（纯前端，零后端契约变更，沿用 `/api/projects/{pid}/stats`）**：
  - `api.service.ts` 新增 `getProjectStats(pid)`（日创建/日完成时间序列 + 汇总计数）。
  - `app.routes.ts` 新增 `stats` 路由；`nav-bar.html` 新增「统计」链接。
  - 新组件 `stats/{ts,html,css}`：5 张汇总卡片（任务总数/已完成/进行中/待办/完成率）+ 纯 CSS 双系列柱状图（创建 vs 完成），日/周/月分段切换（day 用原日、week 取周一、month 取 YYYY-MM，避免 toISOString UTC 偏移）；项目下拉框（默认全部项目 → forkJoin 并行聚合各 project stats）。
- **验证**：`ng build admin-portal` 通过（stats chunk 9.72kB）；`tests/test_admin_portal_stats_e2e.py` 全绿（汇总卡片 5 张、日/周/月切换均有柱、切换项目 id=3 → total=164、导航高亮、0 pageerror/console/.js+.css 404、无预期外 401）。回归：登录 E2E 全绿、`pytest test_epic30_cache.py` 8 passed。
- **关键坑（已解决）**：① 02:04 运行的 `ng serve` 残留进程仍占 IPv4 `127.0.0.1:4300`，本次新 serve 默认绑 IPv6 `[::1]`，Playwright 走 127.0.0.1 命中**旧代码**（无 stats 路由 → 重定向 /login）。解决：PowerShell `Stop-Process` 按 4300 端口所有者杀掉残留 serve + `rm -rf .angular/cache` + 重启 `--host 127.0.0.1`。② 状态 PUT 首次遇 API 58125 瞬时 HTTP 000，重试链 `todo(400)→in_progress(200)→in_review(200)` 成功，终态 in_review。
- **状态（REST）**：Task 854 `backlog→in_review`；Story 71 → in_review；Epic 32 仍 backlog（Story 72 E2E 仍 backlog，不误标）。
- **提交**：`a111025` `feat(ui): admin-portal 统计页 - 任务趋势日/周/月聚合 + 汇总卡片 (Task 854 -> in_review)` → push origin main 成功。本次 git add 仅 admin-portal 源码树 + 新 E2E 测试（补齐 02:04 未提交的 users/projects/nav-bar 源码，使仓库 admin-portal 可构建），刻意排除 data/、autodev.lock、其他 automation 的 memory、screenshots、前端 dist、scratch 脚本。
- **端口纪律**：未触碰 18001(MCP)/docker；API 58125、web 8080/8090 原样；验证后已停 4300 残留 serve。

## admin-portal E2E 测试骨架收拢（Task 857-861 → in_review，达成）
- **目标**：本次 task → in_review。MCP 连接器全断 → REST 兜底（API 58125，admin id=18）。
- **选型**：admin-portal Story 71（前端实现，850-856）已全部 in_review；Story 72（Playwright 自动化，857-861）仍为 backlog，是最高优先级真实 backlog → 收拢为统一可独立运行的 E2E 骨架。
- **实现（纯测试基础设施，零前端/后端契约变更）**：
  - `scripts/serve_admin_portal.py`：`ThreadingHTTPServer` 静态托管 `frontend/dist/admin-portal/browser` + `/api` 反向代理到 `http://127.0.0.1:58125`（同源代理 → 无 CORS 干扰）；非 `/api` 且无扩展名路径 SPA 回退到 `index.html`。
  - `tests/admin_portal/` 包：`__init__.py`（说明）+ `_harness.py`（共享 `start_browser()`/`login_ui()`/`check_errors()`/`report()`）+ `run_all.py`（编排三用例，非零退出）+ 三个用例（login / users_projects / stats），`BASE` 改读 `ADMIN_PORTAL_URL` 环境变量（默认 `http://127.0.0.1:4321`）。
  - 根级散落脚本 `tests/test_admin_portal_{login,stats,users_projects}_e2e.py` 经 `git mv`/移动收拢至 `tests/admin_portal/`，去掉硬编码 4300、脱离手动 `ng serve`。
- **验证**：`ng build admin-portal --configuration development` 通过（login/stats/users/projects/dashboard 懒加载 chunk 正常）；`python scripts/serve_admin_portal.py --port 4321 &` + `python tests/admin_portal/run_all.py` 全绿 —— login PASS / users_projects PASS(users=51, projects=50) / stats PASS（5 汇总卡片、创建·完成双系列柱状图、日周月聚合切换、项目 3 下拉切换刷新；全程 0 pageerror/console/.js+.css 404，无预期外 401）。后端回归 `pytest test_epic30_cache.py` 8 passed（零源码改动）。
- **状态（REST 合法链 `backlog→todo→in_progress→in_review`）**：Task 857/858/859/860/861 → in_review；`PATCH /api/stories/72` Story 72 → in_review；`PATCH /api/epics/32` Epic 32 → in_review（两 story 均 in_review，Story 71 亦 in_review）。
- **坑（已解决）**：① `serve_admin_portal.py` 初版 `global TARGET` 声明在 `main()` 内 `TARGET` 首次使用（default=TARGET 读模块全局）之后 → Python 报 "used prior to global declaration" SyntaxError；将 `global TARGET` 提到 `main()` 首行修复。② Git Bash 的 `/tmp` 与 Python 的 `/tmp` 路径不一致（mingw vs E:\tmp）→ token 文件读不到；改用环境变量 `AB_TOKEN` 传递。③ `taskkill //PID` 在 Git Bash 被转义失败 → 改用 PowerShell `Stop-Process -Id` 停 4321 服务。
- **提交**：`132d62a` `feat(test): admin-portal E2E 测试骨架 - 统一静态代理服务 + 共享 harness + 编排器 (Story 72 / Task 857-861 -> in_review)` → push origin main 成功（`3504036..132d62a`）。git add 仅 11 个交付文件（3 openspec + serve 脚本 + 6 测试文件 + 2 旧根级测试删除），刻意排除 data/、autodev.lock、其他 automation 的 memory、screenshots、前端 dist、agentboard-audit/、scratch 脚本。
- **端口纪律**：未触碰 18001(MCP)/docker/8080/58125/8090/28080；自建验证服务用 4321（验证后已停）。
- **下次可执行**：admin-portal 全部 Story（71 前端 + 72 E2E）均 in_review → Epic 32 可标 done；或转向其它高优先级需求。


## 任务列表行密度切换补齐（Epic 66 v5.3，Task 1121 -> in_review）
- **背景**：listDensity 信号（app.ts:90，读 localStorage['agentboard_list_density'] 默认 comfortable）、toggleListDensity()（app.ts:2884）、.entity-list.density-compact 紧凑样式（app.css:973，行内边距 10px->6px）早已存在，但任务视图工具条无触发按钮 -> 功能半成品。本次补齐按钮使能力真正可用。
- **实现（纯前端，零后端契约变更）**：app.html filterbar__right「折叠」开关后新增 #densityToggle 按钮（调 toggleListDensity()，文案舒适/紧凑 + aria-pressed）；app.css 补 .btn.density-toggle（.density-glyph + aria-pressed=true 高亮）。组件作用域 CSS 编译进 main.js 运行时注入（不在全局 styles.css）。
- **验证**：tests/test_epic66_v53_row_density_e2e.py 全绿。断言改用 getComputedStyle(el).paddingTop（规避字体加载/亚像素抖动），点击后等 transition: all .2s 完成（350ms）再测。覆盖：默认舒适(10px)->切紧凑(6px)->恢复(10px)->刷新持久化（localStorage='compact'+类+文案）。0 pageerror/console/.js+.css 404。回归 pytest test_epic30_cache.py 8 passed + E2E v5.2 bulk_duplicate 全绿。
- **状态（REST 合法链）**：Task 1121、Story 105、Epic 56 均 in_review。
- **可复用经验**：① 测 CSS 过渡必须用计算样式 + 等过渡结束，否则测到动画中间帧（如 padding 9.1px）；② locator.evaluate 用 (el)=>... 箭头函数接收元素，不能用 arguments[0]；③ 组件作用域样式进 main-*.js，验证应 grep dist/main-*.js 而非 styles.css。
- **端口纪律**：未触碰 18001(MCP)/docker；API 58125、web 8080/8090/28080/18000 原样。
