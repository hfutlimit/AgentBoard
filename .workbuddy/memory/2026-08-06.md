# 2026-08-06 工作日志

## Epic 64 S1 腾讯云 COS 上传后端 → Task 991 in_review（达成）

- **背景**：项目 3 当前唯一未完成高优 Epic 为 **Epic 64（腾讯云 COS 图片上传与图片展示）**，4 个 Story（61/62/63/64）全部 backlog。本次交付地基 Story 61：**腾讯云 COS 上传后端（配置占位 + 优雅降级）**，为后续 S2 文档 / S3 评论 / S4 Story·Epic 描述图片提供可复用上传能力。
- **选型**：MCP 连通，list_epics 项目 3 所有 in_progress Epic = 仅 Epic 64 backlog。最高优先级独立可交付单元 = Story 61，新建 Task 991（highest）。
- **方案对比**：
  - A. 官方 SDK cos-python-sdk-v5（引入 ~10 个传递依赖，Docker/api 需 pip install + dist 打包同步 requirements，**双后端部署成本高**）
  - **B. 纯标准库自实现 COS V5 签名（选定）**：腾讯云官方算法是公开的 HMAC-SHA1（HMAC-SHA1(SecretKey, KeyTime) → SignKey；SHA1(HttpString) → StringToSign="sha1\\n{KeyTime}\\n{SHA1(HttpString)}\\n"；HMAC-SHA1(SignKey, StringToSign) → q-signature）。**零新增依赖**，SQLite/MariaDB/Windows IIS/dist 全环境免安装。
- **交付**：
  - `agentboard/cos_client.py`（新增，纯标准库 7980 字节）：`CosClient(env=...)` + `is_configured/config_dict/host` + `_sign(method, path, params, headers, key_time, sign_time)` + `_build_authorization` + `_build_presigned_query` + `put_object(key, data, content_type, now_ts=, urlopen=)`（PUT 直传，可注入 urlopen 测试） + `presigned_get_url(key, expire_sec=86400, now_ts=)` + `CosError`。
  - `agentboard/api.py`：import `from .cos_client import client as _cos_client, CosError`；新增 `GET /api/projects/{pid}/cos/config`（200 即使未配置 → `configured:false`）、`POST /api/projects/{pid}/cos/upload`（项目 404 / COS 未配置 503 / 超 10MB 422 / 非图片 MIME 422 / 成功 201）。端点路径 `^/api/projects/(\d+)` 被 `project_access_middleware` 自动覆盖（成员写 / admin 绕过 / 匿名 401）。
  - `tests/test_cos_upload.py`（18 passed）：
    - **TestCosClient 纯单元**：签名与官方四步参考实现对比（独立手算，算法自洽核心护栏）、预签名 URL 结构（7 个 q-* 参数、字母序、keep_blank_values 解析）、PUT 请求构造（mock urlopen 断言 Authorization/Host/Content-Length/Content-Type）、非 2xx 抛 CosError、未配置抛错。
    - **TestCosApi 同进程直调**（monkeypatch `_cos_client`）：config 200 configured:false、404/503/422、201 成功（FakeClient 断言返回结构 + key 前缀 uploads/{pid}/）。
    - **TestCosAuth uvicorn 子进程**（REQUIRE_AUTH=1）：成员 config 200、未配置 upload 503、anon 401、非成员 403。
  - OpenSpec `openspec/changes/cos-upload-epic64-s1-20260806/`（proposal / design / tasks 三件套）。
- **验证**：
  - **测试**：`tests/test_cos_upload.py` 18 passed。
  - **聚焦回归**：`test_cos_upload + test_crud_smoke + test_domain_boundaries` = 21 passed / 9 skipped / 0 failed。
  - **部署冒烟**（Docker api 18000，`tmp/cos_smoke.py`）：login admin → config 200 configured:false + 明确 error → upload 503 + 完整提示 → anon 401 → /api/meta 200（**其余功能不受影响**）。
  - **Playwright E2E**（Docker web 28080 + route 重写 API 到 18000，`tmp/cos_e2e_smoke.py`）：admin 登录 → 项目 3 仪表盘正常渲染（100 项目/83 Epic/158 Story/327 任务/48% 完成率 + 交付态势图 + 任务分布/项目进度）→ **0 console error / 0 pageerror / 0 failed js·css 请求** → 截图 `tmp/cos_e2e_smoke.png`。
  - **部署**：启动 Docker Desktop（daemon 初始未运行）→ 所有容器自动恢复（含 agentboard-mcp-1 18001，Docker Desktop 自动拉起，**非我主动操作**）→ `docker compose restart api`（**仅 api 服务，不动 mcp**）→ curl /api/meta=200 验证就绪。
- **坑（关键）**：
  1. **COS V5 签名是四步算法**，不是直接对 HttpString 做 HMAC！官方 StringToSign = `sha1\\n{KeyTime}\\n{SHA1(HttpString)}\\n`；SignKey = HMAC-SHA1(SecretKey, KeyTime)；Signature = HMAC-SHA1(SignKey, StringToSign)。初版实现直接对 HttpString 做 HMAC 是错的，搜索官方文档（cloud.tencent.com/document/product/436/7778）后修正。
  2. `urllib.request.Request.headers.items()` 返回的 header 名被 `.capitalize()`：`content-type`→`Content-type`、`authorization`→`Authorization`。测试断言统一小写。
  3. `Authorization` 参数按**字典序**排列（与 cos-python-sdk-v5 SDK 行为一致：sorted(authorization.items(), key=...)）。文档展示顺序是 `q-sign-algorithm&q-ak&...&q-signature`，但**实际字典序**是 `q-ak&q-header-list&q-key-time&q-sign-algorithm&q-sign-time&q-url-param-list&q-signature`（服务端解析后重建，无影响）。
  4. `urllib.parse.parse_qs` 默认 `keep_blank_values=False`，会丢弃 `q-url-param-list=` 这种空值参数。断言需加 `keep_blank_values=True`。
  5. PUT 请求签名 TTL 用 `_REQUEST_TTL_SECONDS=60`（与预签名 86400 不同），不要混用。
  6. **Git Bash 无 `tee`/`seq`/`sleep`** 命令（被 Windows 同名词替换/缺失）→ 用 Python 子进程 `time.sleep` 代替。
  7. **Docker Desktop 启动时所有容器自动恢复**（含 mcp 18001），属环境自动行为而非主动操作；如需避免，单独 `docker compose up -d api` 而非重启整个 compose stack。
- **状态**：MCP Task 991 → **in_review**（合法链 backlog→todo→in_progress→in_review）。Story 61 维持 backlog（本轮范围仅交付后端，前端 markdown 渲染管线在后续 S2/S3/S4 接力时联动）。
- **提交**：`ce48d59`，push origin main 成功（`9e70415..ce48d59`，5 文件 +798 行）。
- **硬约束**：✅ 未触碰 18001/docker mcp 容器；✅ 零既有 REST/DB 契约变更（仅新增 2 端点）；✅ 零新增依赖（requirements.txt 未改）。

## 下次可执行
- **Epic 64 S2/S3/S4**（Story 62 文档图片 / 63 评论图片 / 64 Story·Epic 描述图片）— 纯前端 markdown 渲染管线增强，复用本次 S1 的预签名 URL。最小可交付单元选 **S2 文档图片**（独立 Story、风险最低、与 Epic 15 文档模块直接复用 `.comments-card`/markdown 渲染层）。
- 或新 Epic：Agent 隔离（项目级 vs Agent 级记忆分区）、文档 v2、Proposal P4 增量。