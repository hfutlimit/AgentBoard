# 2026-08-03 工作日志

## 仓库清理 & git 工作流
- 清理 `screenshots/`（55 张 E2E 验证图）、`tests/screenshots/settings.png`、`scripts/verify_*/*.png` 等历史测试副产物，共删 63 文件 → commit `5482451`。
- 清理 `docs/sprint_ui_*.png`（13 张未引用 sprint review 图）、`docs/debug_spa_state.png`，保留 `docs/design/shots/*`（被 openspec 引用为重设计前基线），共删 12 文件 → commit `37859ab`。
- 两次推送均成功 `push origin main`。

## 测试产物约定落地
- 新建 `tmp/` 目录与 `tmp/.gitkeep` 占位。
- `.gitignore` 增加：
  - `tmp/` / `tmp/**`：测试脚本运行期截图/dump 容器
  - `screenshots/` / `tests/screenshots/` / `scripts/*.png`：脚本运行副产物，禁止入库
- 写入 `MEMORY.md` 的"测试产物约定"段落，明确新脚本须写 `tmp/<module>/<case>.png`，禁止 `git add` 强行追踪历史遗留路径。

## 待办
- 41 个 `tests/*.py`、`scripts/*.py` 仍在仓库里、运行时仍会生成 PNG，但因 `.gitignore` 已加 `screenshots/**`、`scripts/*.png` 等模式，运行产物不会被 git 看到，无需立刻改代码。
- 历史一次性脚本（`tests/debug_spa.py`、`tests/shot_epic76_v63.py`、`tests/test_sprint_ui_review.py`）含绝对路径 `E:/Projects/WorkBuddy/AgentBoard/...`，会写到工作区之外的位置。可在下次任务顺带改写为 `tmp/...`，本次为降低风险未动。