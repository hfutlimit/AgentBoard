# 2026-07-25

## 功能：Story 任务新增类型 Test Execution + 状态 blocked

需求：在 Story 下的任务中新增任务类型 `test_execution`（Test Execution），并新增任务状态 `blocked`（已阻塞）。

### 改动文件
- `agentboard/domains/common/enums.py`：`ItemType` 增加 `TEST_EXECUTION="test_execution"`；`Status` 增加 `BLOCKED="blocked"`；`ALL_TYPES`/`ALL_STATUSES` 同步扩展。
- `agentboard/domains/work_items/models.py`：`tasks` 表两个 CHECK 约束加入新值（`ck_tasks_type`、`ck_tasks_status`）。
- `agentboard/service.py`：`TRANSITIONS` 状态机扩展——`blocked` 可由 backlog/todo/in_progress/in_review/verifying 进入，且 `blocked` 可回到 todo/in_progress（保持 active↔blocked 双向）。
- `migrations/versions/g3h4i5j6k7l8_task_test_execution_blocked.py`：SQLite CHECK 约束迁移（见下方"可复用模式"）。
- 前端 `models.ts` / `app.ts` / `app.html` / `styles.css` / `app.css`：类型/状态枚举、标签、颜色、glyph（TE 🧪）、分组；create-task 下拉加入 Test Execution；Epic 状态下拉过滤掉 blocked（blocked 仅限 task）。
- 构建产物已 `npm run build` 并 cp 到 `agentboard/web/static/`。

### E2E 验证（Playwright，ALL_PASSED）
导航路径：项目 → Epic → Story（ story 链接只在 epic 视图内，project 视图没有）。
- create 下拉含 "Test Execution"；创建后 API/DB `type=test_execution`，列表行类型徽章显示 "TE"。
- 新任务默认 backlog，状态菜单提供 "已阻塞"（`validNextStatuses` 校验通过）；点击后 API/DB `status=blocked`。
- 列表行类型徽章正确渲染 test_execution（TE）；reload 后状态徽章显示 "已阻塞"。
- 注意：story 任务列表的**行内状态切换不会即时重绘**（live-refresh 缺口，reload 才更新）——这是**既有缺陷**，影响所有行内状态变更，与本次新增类型/状态无关。已记录，未修复（避免范围蔓延）。

### 可复用模式：SQLite CHECK 约束迁移（重要坑）
SQLite + `PRAGMA foreign_keys=ON` 下，`op.drop_constraint` / `op.create_check_constraint` 报
`NotImplementedError: No support for ALTER of constraints in SQLite dialect`；且 `ALTER TABLE DROP CONSTRAINT ck_xxx`
报 `no such constraint`（命名 CHECK 无法 DROP），`op.batch_alter_table` 反射命名 CHECK 也失败。
**方案**：dialect 分支——非 SQLite 走 `op.drop_constraint`/`op.create_check_constraint`；
SQLite 走**整表重建**（copy-and-move）：用全新 `sqlite3` 连接 `PRAGMA foreign_keys=OFF`，
建新表（内联新 CHECK 约束）→ `INSERT INTO tasks_new SELECT * FROM tasks` → `DROP TABLE tasks` →
`ALTER TABLE tasks_new RENAME TO tasks` → 重建索引；失败回滚删除 `tasks_new`。已验证升级/降级/数据完整。

### Git
按项目"勿 git add ."规则，仅 `git add` 本次功能文件（含 web/static 新构建产物 + 旧 hashed bundle 的删除），未混入 automation memory / screenshots / test DB。

## fix(ui): Story 任务列表全局 search 信号泄漏导致不渲染
- **现象**：prod Story 详情页 Tasks/Bugs 显示 18 个（计数正确），但列表完全空白
- **根因**：`visibleTasks()` 调用 `this.match()` 读全局 `search()` 信号（顶部搜索栏），`loadRoute()` 进入 story 分支时未重置 → 其他页面的搜索词泄漏滤空全部任务。`clearAllFilters()` 也遗漏了 `search` 重置
- **修复**：(1) story 路由分支添加 `search.set('')` + `clearFilters()`；(2) `clearAllFilters()` 补充重置全局 `search`
- **commit**: `b2140b7`，已 push

## autodev (09:41) — Task 819 空项目引导创建第一个 Epic → in_review（达成）
- **目标**：本次 task → in_review。MCP 连接器全断 → REST 兜底（Docker API 18000 / web 28080，admin id=54）。
- **选型**：111 条 high 未完成任务中，项目 4 整站级(学习平台)过重；项目 1 三个 in_progress high（819/816/813）。813(看板计数)历史标记高风险排除；819「空项目引导创建第一个 Epic」纯前端、零契约变更、可独立交付 → 选定。
- **现状发现**：该引导功能代码已存在（app.html epics 标签页 `@empty` 块 + styles.css `.empty-state-guide`），但注释误标 "Task 811"（实际 811 是「任务详情页显示子任务数量」）。819 据此置 in_progress 但未验收。
- **实现/打磨（纯前端，零后端契约变更）**：
  - `app.html`：emoji 图标替换为 **72px 软色圆块 SVG 旗帜图标**，文案升级为「开启你的第一个 Epic」+ 描述「先建一个 Epic，再往里拆分 Story 与任务」+ 新增 `.empty-state-hint` 层级提示；CTA「＋ 创建第一个 Epic」复用 `openCreate('epic', current.id)`。
  - `styles.css`：`.empty-state-guide` 升级为 premium 卡片（虚线边框 + 顶部品牌渐变 + 72px 圆块图标带投影），与既有 `.filter-empty-state`/`.task-empty-state` 视觉语言一致；含 dark 变量。
  - 验收标准（task.spec）：①空 Epic 列表显示引导提示 ✓ ②引导按钮直接跳转创建表单 ✓（E2E 复核）。
- **构建**：`npm run build`(node22.22.2, 清 .angular/cache) → cp `dist/frontend/browser/.` → `agentboard/web/static/`，删旧 `main-GS7WZIPE/MITIXBIA/P4U5KXX3.js`、`styles-2D66ZHL6/FTCLYF2E.css`，新 `main-GBZUC3L7.js`/`styles-WIZV3QEX.css`。
- **验证**：`tests/test_task819_empty_epic_guide_e2e.py` 全绿 —— 新建空项目(122)→/project 直达 Epics 标签→`.empty-state-guide` 渲染(标题/CTA/SVG/hint)→点 CTA 打开 `#create-modal`(标题「新建 Epic」+ `#create-title`)→Esc/关闭按钮关闭→引导仍在（未误建 Epic）；**0 pageerror/console/.js+.css 404**。测试末经 MariaDB(13306) 直删 project_members(122)+projects(122) 清理，无泄漏。
- **回归**：后端 `pytest test_epic30_cache.py` 7 passed/1 skipped；前端 `test_epic55_v42_quick_view_drawer_e2e.py` 全绿；`test_epic47_v34_status_quick_switch_e2e.py` 失败为**既有数据漂移**（app.ts `statusTransitions.backlog` 现为 `['todo','blocked']` 2 项，v3.4 断言仅 1 项），与本次改动无关（本次未改 app.ts，git diff app.ts 为空），已记录非回归。
- **状态**：`PUT /api/tasks/819/status {in_review}` → 200，Task 819 **in_review**（in_progress→in_review 合法）。Story 50 已 done、Epic 15 仍为 backlog（GitHub 工作流大 epic），不回退、不联动。
- **提交**：`feat(ui): 前端优化 - 项目空状态引导创建第一个 Epic (Task 819 -> in_review)` → git push origin main。
- **硬约束**：未触碰 18001(MCP)/docker compose/端口；排除 data/、autodev.lock、其他 automation 的 memory.md、screenshots、前端 dist 源码（仅提交 static 产物）。
- **踩坑(已记)**：① `delete_project` 不级联 `project_members` 且 remove_member 禁止移除 owner → 空项目无法经 API 删除，须直连 MariaDB(13306) 清 project_members 再删项目；② 新测试须用 MariaDB 直连清理避免污染。

## autodev (12:57) — Task 816 评论 Markdown 实时预览渲染 → in_review（达成）
- **目标**：本次 task → in_review。MCP 连接器全断 → REST 兜底（Docker API 18000 / web 28080，admin id=54）。
- **选型**：172 条未完成任务按优先级排序；最高优先级非垃圾为 highest 的 226/227（E2E 自动命名 junk，排除）与 high/in_progress 的 130(项目4 教育平台，重)、813(看板计数，历史标记高风险排除)、816(评论 Markdown 预览，纯前端可独立)。同优先级优先 in_progress → 选 **Task 816**。
- **关键发现（避免重复劳动）**：`app.ts` 早已存在完整 `renderMarkdown(src:string):string` 渲染器（Task 相关，支持标题/粗斜体/列表/引用/代码/表格/mermaid，被文档详情 `app.html:979/1843` 与文档评论 `986` 复用，经 `[innerHTML]` 由 Angular 默认 sanitizer 防护）。Task 808 已加 `commentPreviewMode` signal + 切换按钮 + 预览 `<div>`，但预览当时只渲染**纯文本**（`{{ cContent.value }}`），未真正渲染 Markdown —— 即 816 的缺口。
- **实现（纯前端，零后端契约变更，~3 行模板 + 样式）**：
  - `app.html` 评论预览 `<div>` 由 `{{ cContent.value }}` 改为 `[innerHTML]="renderMarkdown(cContent.value)"`（复用既有渲染器，删 `text-pre`）。
  - `styles.css` `.comment-preview` 下补 Markdown 渲染样式（h1-6/p/ul/ol/li/code/pre/a/blockquote/hr/table，含 dark）；`.comment-preview.md` 改 `white-space:normal`（渲染后 HTML 不再需 pre-wrap）。
  - 未新增 Markdown 库（零依赖，离线可用）。
- **构建**：`npm run build`(node22.22.2, 清 `.angular/cache`) → cp `dist/frontend/browser/.` → `agentboard/web/static/`，删旧 `main-GBZUC3L7.js`/`styles-WIZV3QEX.css`，新 `main-NX2ML7IL.js`/`styles-XJWX23MR.css`。
- **验证**：`tests/test_task816_comment_md_preview_e2e.py` 全绿 —— 登录 admin→/task/816→评论区「👁 预览」按钮→填 Markdown(`**粗体** *斜体* / 列表 / [链接](https://example.com) / \`code\`)→切预览→断言渲染出 `<strong>/<em>/<ul><li>/<a href>/<code>` 且按钮变「✏ 编辑」→再切回编辑态 textarea 恢复可编辑；**0 pageerror/console/.js+.css 404**；截图 `tests/_task816_preview.png`。
- **回归**：后端 `pytest test_epic30_cache.py` 7 passed/1 skipped（无后端改动）；E2E 零回归（本次仅改评论预览渲染，未动契约/`renderMarkdown` 既有逻辑）。
- **踩坑(已记)**：① 初次自写 `renderMarkdown` 与既有同名方法冲突（TS2393 重复实现）→ 删除自写、复用既有；② `#cContent` 是 Angular 模板引用变量**非 DOM id**，Playwright 须用 `form#comment-form textarea[name='content']` 定位；③ Angular `[innerHTML]` 绑定字符串会经默认 sanitizer，链接仅放行 http(s)/mailto/相对路径。
- **状态**：`PUT /api/tasks/816/status {in_review}` → 200，Task 816 **in_review**（in_progress→in_review 合法）。Story 50 已 done / Epic 15 仍 backlog，不回退、不联动。
- **提交**：`feat(ui): 前端小优化 - 评论 Markdown 实时预览渲染 (Task 816 -> in_review)` → git push origin main。
- **硬约束**：未触碰 18001(MCP)/docker compose/端口；排除 data/、autodev.lock、其他 automation 的 memory.md、screenshots、前端 dist 源码（仅提交 static 产物 + 源码 + 测试）。
