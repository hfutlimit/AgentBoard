# 2026-08-08 部署 + 完整测试

## 部署（docker，dev 模式 bind 挂载）
- Docker 守护未运行 → 启动 Docker Desktop.exe 自动拉起全套容器（api:18000 / web:28080 / mcp:18001 / db:13306）。
- 用 managed Node 22 构建前端：`frontend/dist/frontend/browser/` 才是产物目录（非 `frontend/dist/frontend/`）。
  ⚠️ 误把 `browser/` 整目录 cp 进 `agentboard/web/static/` 产生残留 `static/browser/`（safe-delete 拦截删除失败，目录无害、未被引用，保留）。正确：`cp -r frontend/dist/frontend/browser/. agentboard/web/static/`。
- 重启 `agentboard-api-1` + `agentboard-web-1`（**不碰 18001 MCP**）。验证：api /api/meta=200、web /=200 且线上 bundle=main-63G32IC4.js（与当前源码一致，源码自上次部署未变）。

## 测试结论
- **后端逻辑 pytest（容器内，排除 Playwright/admin_portal/脚本式文件）**：450 passed。失败全部为环境/陈旧，无产品回归：
  - 🔴 真实部署隐患：`agentboard/scheduler.py` 导入 `croniter` 但 **api 镜像缺该包**（requirements 已列但未进镜像）→ 已在运行容器 `pip install croniter`（重启保留，未重建镜像）。修复后 scheduler 套件 30 passed。
  - test_epic98（需 dist/ 产物，容器无）、test_docs_*（需 /app/docs，未挂载）、test_crud_smoke/test_epic42/45/test_review_87_92（需 live server，属 E2E 范畴）→ 环境缺失。
  - test_task_estimate::test_estimate_in_task_list：测试陈旧（把 `GET /api/stories/{id}/tasks` 当 list 迭代，实际返回 `{items,total}`）→ TypeError。非产品缺陷。
  - test_epic30_cache：ImportError（疑似容器缺可选依赖），待查。
- **前端 vitest**：37 passed（app.spec 34 + pagination 3）。
- **浏览器冒烟（针对本机 docker 28080/18000，受管 venv playwright + 已装 chromium）**：PASS — admin 登录 200、仪表盘/概览统计(Epic117)渲染、侧栏导航就绪、**0 console/page error**。
- 仓库 E2E 套件（100+ *_e2e.py）硬编码目标 `127.0.0.1:8090 / :58125`，**无法直接套用本机 docker(28080/18000)**，未跑全套；以浏览器冒烟替代验证部署可用性。

## 后续建议
- 重建 api 镜像固化 croniter（否则容器 recreate 后丢失）。
- 修 test_task_estimate 列表断言 + 给 E2E 增加可配置 BASE_URL 环境变量。
- 清理 static/browser 残留（需绕过 safe-delete 或直接忽略）。

## gitignore 补充 + 推送（同日收尾）
- 用户要求忽略测试产物并 push。扩充 `.gitignore`：新增 `.pytest_cache/`、`**/.pytest_cache/`、`.coverage`/`coverage.xml`/`htmlcov/`、`playwright-report/`/`test-results/`/`.playwright/`、`.mypy_cache/`/`.ruff_cache/`/`.tox`/`.nox/`、`.workbuddy/autodev.lock`、前端误拷入的 `agentboard/web/static/browser/`。
- 核验：`.pytest_cache`/`static/browser`/`autodev.lock` 均未被跟踪且已被 ignore 规则命中；本地根目录确存在 `.pytest_cache`（先前遗留，现已忽略）。
- 精准提交（未 `git add .`，避开 dist/、.workbuddy/automations、其他日期 memory、agentboard-audit/data/deliverables 等预存脏文件）：`.gitignore` + `2026-08-08.md` + `MEMORY.md`（上一轮精简重组的长期记忆）。commit `28f6f1d`，`git push origin main` 成功（d7da65c..28f6f1d）。
- 注意：static/ 根堆积大量未跟踪陈旧 `main-*.js`/`styles-*.css`（构建副产物残留），不在本次范围，可后续清理。

## 状态机全量梳理（用户咨询，源码核对）
- 已把 MEMORY.md「API / 状态机」段更新为权威版：含 BLOCKED 的完整 TRANSITIONS、Bug==Task、Epic/Story 无强制迁移、Story 评审词表（pending_review/ready）、评审闭环 CAS 流与 MAX_REVIEW_ROUNDS=5。
- 源码位置：`agentboard/domains/common/enums.py`（Status 7 值）、`agentboard/service.py`（TRANSITIONS L49-57、评审函数）、`agentboard/domains/projects/models.py`（STORY_REVIEW_STATUSES + ck_epics_status/ck_stories_status）、`agentboard/domains/work_items/models.py`（ck_tasks_status）。
- 发现潜在缺陷：Epic `_check_status` 接受 blocked 但 DB CHECK 不含 → PATCH epic=blocked 会 IntegrityError。

## Epic 123：设计评审流 + blocked 全向/历史 + needs_design（已实施部署）
- 需求（用户）：新增状态链 backlog→todo→[in_design→design_pending_review→design_review_approved]→in_progress→in_review→final_review→done；按 Story.needs_design 分支（默认 true 走设计段，false 快速流 todo→in_progress）；**任意状态可置 blocked、记住 previous_status 解除恢复、状态变更落 history**。
- 决策（AskUserQuestion）：现有状态机**扩展**（保留 7 态 + verifying/blocked），存量零迁移；needs_design 默认 true。
- 实施：enums.py +4 态；Task.previous_status + TaskStatusHistory 表；Story.needs_design（默认 true）；service.py `transitions_for(needs_design)` 动态迁移表（true 时 TODO→IN_DESIGN 必须、禁止直跳 IN_PROGRESS；false 快速流；后段 in_review→final_review→done 共用；done→blocked 也允许）；set_status/batch/claim/review/多数决/超时全部写 history（_record_status_history）；API：StoryIn/Patch needs_design、StatusIn.reason、GET /api/tasks/{tid}/status-history、MCP create_story/update_story needs_design。
- 迁移：`r7s8t9u0v1w2`（列+CHECK+history 表）、`r8s9t0u1v2w3`（**状态列宽 20→40**，design_pending_review 21 字符超 VARCHAR(20) → MariaDB 1406 Data too long；SQLite 不强制所以单测没暴露，真实环境踩坑）。
- 测试：tests/test_epic123_design_flow.py 6 passed（service 层）；真实 API 冒烟 tmp/epic123_smoke.py 9/9（设计链全通+blocked 恢复+快速流拦截+history 端点）；UI 冒烟 tmp/epic123_ui_smoke.py PASS（4 新状态列渲染+0 报错）。
- ⚠️ 本机端口踩坑：宿主有**遗留进程监听 [::1]:18000**（旧代码实例），curl/python localhost 优先走 ::1 会打到旧实例 → 一律用 127.0.0.1 显式 IPv4；urllib 还要 ProxyHandler({}) 禁系统代理。api 容器 bind mount migrations 只读，docker cp 会失败但本地文件自动可见。
- 部署：alembic upgrade head 两次 + docker restart api web（不碰 18001）；线上 bundle main-2DSFJRYJ.js；/api/meta statuses 11 个。
