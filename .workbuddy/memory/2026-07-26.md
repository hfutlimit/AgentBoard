# 2026-07-26 自动开发日志

## Epic 58 v4.5 快速查看抽屉任务前后导航（task 1108 → in_review，达成）

- **背景**：v 系列（v1.5~v4.4）任务列表/抽屉已高度完整；admin-portal（850-861,high）整站级、1 小时内不可独立收尾 → 延续 v 系列增量，补齐「抽屉内上一项/下一项」Jira/Linear 式 triage 体验。纯前端、零后端契约变更。
- **追踪（REST 新建，MCP 连接器断开）**：project 43(AUTO58)→epic 48(Epic 58 v4.5)→story 97(Story 58.1)→task 1108(high)；状态链 `backlog→todo→in_progress→in_review`；story 97、epic 48 经 PATCH 同步 **in_review**（本次「task → in_review」目标达成）。
- **实现**：
  - `app.ts`：新增 `qvHasPrev()`/`qvHasNext()`（基于 `visibleTasks()` 当前索引判边界）+ `qvNav(delta)`（复用 `openQuickView` 切换）+ `onDrawerKeydown()`（`[` 上一项 / `]` 下一项，输入框聚焦不触发）；`openQuickView` 切换任务时重置 `qvEditingTitle`/`qvEditingDesc`（修 v4.3 编辑态串台隐患）。
  - `app.html`：抽屉头部新增 `.qv-nav-group`（‹/› 按钮，边界 `disabled`），`<aside>` 绑 `(document:keydown)="onDrawerKeydown($event)"`。
  - `app.css`：`.qv-nav-group`/`.qv-nav`（含 dark），复用 `.qv-close` 视觉。
- **验证**：`tests/test_epic58_v45_drawer_nav_e2e.py` 全绿（按钮 next/prev + 键盘 `[`/`]` + 边界禁用 + Esc 关闭；0 pageerror/console/.js+.css 404）；`pytest test_epic30_cache.py` 8 passed。
- **关键发现（已定位，非本次回归）**：本地 web 8080 由 web_app.py 以默认 `AGENTBOARD_API_URL=http://127.0.0.1:58124` 启动，而 API 实际在 58125 → 页面登录被拒。本次为 E2E 另起 web 8090（`AGENTBOARD_API_URL=http://127.0.0.1:58125`，managed python 3.13.12 + uvicorn）验证，未触碰既有 8080/18001/18000/28080。
- **关键发现（既有缺陷，非本次回归）**：抽屉「UI 添加评论后列表刷新」在原始代码（git stash 还原后重建）上同样不刷新（抽屉显示 1、服务端已 2），属 v4.4 既有评论列表刷新瑕疵，与 v4.5 导航改动无关（导航不触及评论信号）。本次未修复（既有 E2E 默认指向 Docker 18000/28080，不在本次范围）。
- **构建**：`npm run build`(managed node 22.22.2, 清 `.angular/cache`) → `main-TYYV6Z37.js` cp→`agentboard/web/static/`；删除旧 `main-JM4OL5FW.js`/`main-NX2ML7IL.js`。
- **提交**：feat(ui): 前端小优化 - 任务列表快速查看抽屉任务前后导航 (Epic 58 v4.5) → push 成功。
- **硬约束**：未触碰 18001(MCP)/docker compose/端口；排除 data/、autodev.lock、其他 automation memory.md、screenshots、前端 dist 源码（仅提交 static 产物）、agentboard-audit/。

## 可复用经验
- **本地 E2E web 验证**：若 8080 web 的 `AGENTBOARD_API_URL` 与实际 API 端口不符（登录被拒/ERR_CONNECTION_REFUSED），最干净做法是在新端口起一个 web_app.py（带正确 `AGENTBOARD_API_URL` + 同 static 目录），不扰动既有 8080 服务。
- **回归核对技巧**：用 `git stash` 还原改动→重建原始包→跑同一 E2E，可 100% 区分「本次改动引入」与「既有缺陷」，避免误报回归。注意 `cp dist/.../browser/. dest/` 需加 `-r`，否则子目录（assets）会被跳过、且可能因末尾 `/.` 语法报 "omitting directory"。

## Epic 59 v4.6 筛选预设默认加载自动应用（task 1109 → in_review，达成）

- **背景**：v 系列（v1.5~v4.5）已齐；v4.0 支持多命名筛选预设 + 默认预设（手动「应用默认」），但默认预设未在加载时自动应用。本次补齐「默认预设加载时自动应用」，延续 v 系列增量。纯前端、零后端契约变更。
- **追踪（REST 新建）**：project 45(AUTO59)→epic 49(Epic 59 v4.6)→story 98(Story 59.1)→task 1109(high)；状态链 `backlog→todo→in_progress→in_review`；story 98、epic 49 经 PATCH 同步 **in_review**（本次「task → in_review」目标达成）。
- **实现（含关键时序坑）**：
  - `app.ts`：新增 `applyDefaultPresetOnLoad()`（幂等，`defaultPresetApplied` 标志保证仅应用一次）+ 复用既有 `applyDefaultPreset()`；在 `ngOnInit` 的 `router.events` NavigationEnd 订阅中 `void this.loadRoute().then(() => this.applyDefaultPresetOnLoad())` —— **在 loadRoute 完成（其内部 `clearFilters` 之后）才应用**，避免被路由加载重置。
  - **坑（已解决，重要）**：`loadRoute` 进入 story 视图时 `clearFilters()`（行 1363，防筛选泄漏）。若 `applyDefaultPresetOnLoad` 在 `ngOnInit` 同步或 `setTimeout(0)` 调用，会被 router NavigationEnd 触发的第二次 `loadRoute` 的 `clearFilters` 覆盖（NavigationEnd 是 macrotask，晚于 setTimeout）。须在 `loadRoute().then()`（clear 之后）应用。
- **验证**：`tests/test_epic59_v46_preset_autoload_e2e.py` 全绿（backlog 预设设为默认→刷新→「待规划」chip 自动激活、列表收窄、预设标记默认；0 pageerror/console/.js+.css 404）；回归 `pytest test_epic30_cache.py` 8 passed + v4.5 抽屉导航 E2E 全绿。
- **构建**：`npm run build` → `main-X6ZYPVTO.js` cp→`agentboard/web/static/`；删旧 `main-TYYV6Z37.js`。
- **提交**：feat(ui): 前端小优化 - 筛选预设默认加载时自动应用 (Epic 59 v4.6) → push 成功（`08b5279..1014d1f`）。
- **硬约束**：未触碰 18001(MCP)/docker/端口；排除 data/、autodev.lock、其他 automation memory.md、screenshots、前端 dist、agentboard-audit/。
- **下次可执行**：v 系列可转向「分组/排序维度持久化」「筛选预设含分组/排序可视化标签」或新需求；admin-portal（850-861）仍最高优先级真实 backlog，整站级需更大拆分。

## 可复用经验（追加）
- **Angular 加载时序坑**：`loadRoute` 内 `clearFilters()` 会在每次进入 story 视图时重置筛选；在 `ngOnInit` 同步或 `setTimeout(0)` 设置的「默认预设自动应用」会被 router NavigationEnd 触发的二次 `loadRoute` 的 `clearFilters` 覆盖（NavigationEnd 派发晚于 setTimeout macrotask）。正确做法：在 `router.events` NavigationEnd 订阅的 `loadRoute().then(...)` 中应用，确保晚于 `clearFilters`。

## Epic 60 v4.7 筛选预设可视化标签（task 1110 → in_review，达成）

- **背景**：v 系列（v1.5~v4.6）筛选预设已支持保存/应用/默认预设/加载自动应用；但预设列表项仅显示名称，不展示其捕获的分组/排序维度，用户需逐个应用才能知晓内容。延续 v 系列增量，补齐「预设可视化标签」。纯前端、零后端契约变更。
- **追踪（REST 新建，MCP 全断 → 58125 兜底）**：project 46(AUTO60)→epic 50(Epic 60 v4.7)→story 99(Story 60.1)→task 1110(high)；状态链 `backlog→todo→in_progress→in_review`；story 99、epic 50 经 PATCH 同步 **in_review**（本次「task → in_review」目标达成）。
- **实现**：
  - `app.ts`：新增 `presetGroupLabel(p)`（映射 groupBy→中文标签）、`presetSortLabel(p)`（排序维度 label + ↑/↓ 方向箭头）、`presetFilterCount(p)`（统计激活筛选维度数）。
  - `app.html`：预设项 `.preset-item` 内新增 `.preset-body`（包 name 按钮 + `.preset-meta` 行），渲染 📂 分组 / ↕ 排序(含方向) / ⚲ N 筛选 三个 chip。
  - `app.css`：`.preset-body`/`.preset-meta`/`.preset-meta-chip`（含默认预设高亮配色），复用主题 CSS 变量，dark 自适应。
- **验证**：`tests/test_epic60_v47_preset_meta_e2e.py` 全绿（种子 story 100 + task 1111；设分组=按状态→保存预设→meta chips 渲染「📂 按状态」「↕ 创建时间 ↓」；0 pageerror/console/.js+.css 404；测试末清理种子）。回归 `pytest test_epic30_cache.py` 8 passed + v4.6 preset autoload E2E 全绿（v4.0 presets enhanced E2E 因硬编码 18000/28080 端口+DB 已废弃，属预先存在基础设施失败，非本次回归）。
- **构建**：`npm run build`(managed node 22.22.2, 清 `.angular/cache`) → `main-I3OWIP4M.js` cp→`agentboard/web/static/`；删旧 `main-X6ZYPVTO.js`。
- **提交**：feat(ui): 前端小优化 - 筛选预设可视化标签（分组/排序维度与筛选计数）(Epic 60 v4.7) → push origin main。
- **硬约束**：未触碰 18001(MCP)/docker/端口；排除 data/、autodev.lock、其他 automation memory.md、screenshots、前端 dist 源码（仅提交 static 产物）、agentboard-audit/、scratch 脚本(_track_epic60.py)。
- **下次可执行**：分组维度持久化（taskGroupBy 已持久化至 agentboard_story_group，确认无缺口）、批量指派面板增强、或新需求；admin-portal（850-861）仍最高优先级真实 backlog（整站级）。
- **DB 注意**：本地 uvicorn 58125 库 admin id=18（非 Docker 18000 的 admin 54）；AUTODEV 追踪实体（project 46/epic 50）建在 58125，与 Docker 18000 数据不同步——旧 E2E（v4.0 等）硬编码 18000/28080 已不可在新环境运行。
