# 2026-07-26 工作日志

## 文档模块增强（feat/ui）
- 需求：文档的 Epic/Story 下新增 Task；项目文档点击改为新浏览器标签页打开。
- 实现（单体 App 组件，源码 `frontend/src/app/app.{ts,html,css}`）：
  - 文档详情 `@case ('document')` 新增「关联工作项」卡片：展示 Epic/Story 链接（routerLink）+「＋ 新增任务」按钮。
  - `addTaskFromDoc()`：文档有 `story_id` → `openCreate('task', story_id, project_id)` 直接建任务；仅 `epic_id` → `openCreate('task', undefined, project_id, {epicId})`，弹窗内出现「所属 Story」下拉（取该 epic 的 stories），`create()` 用所选 story 作为 `createTask` 的 storyId。
  - `CreateModal` 增加 `epicId?/storyId?`；新增信号 `createStoryOptions`；辅助 `storyTitle()`。
  - 项目 Tab「文档」列表 tile 与全局 `/documents` 列表 tile 改为 `<a [attr.href]="/documents/{id}" target="_blank" rel="noopener noreferrer">`，点开即新标签页（不再站内导航/内联展开）。
- 构建：`npm run build`（managed node 22.22.2）→ `cp frontend/dist/frontend/browser/* agentboard/web/static/`。
- 验证（Playwright，端口 58124→58125 重定向）：doc1(story) 不显示 Story 选择且任务落入 story1；doc5(epic) 显示 Story 下拉；项目 Tab tile `target=_blank` + `href=/documents/`。12 项全 PASS。
- 提交 `0c7a13a` 并推送 main。验证后已把 doc1/doc5 的测试关联（epic_id/story_id）还原为 NULL。

## 关键坑
- `DocumentPatch` schema 不含 `epic_id/story_id`（仅 `DocumentIn` 含），故无法用 PATCH 关联文档，验证时用直接改 SQLite 造数据。
- 见 MEMORY.md「API 端口偏差」：web 注入 58124、API 实跑 58125。

## Epic 62 v4.9 任务详情页描述 Markdown 渲染 → in_review（达成）
- **目标**：本次 task → in_review。MCP 连接器全断 → REST 兜底（API 58125 / web 8090，admin id=18）。
- **选型**：v 系列（v1.5~v4.8）抽屉已渲染 Markdown（v4.8）；任务详情页 Description/Spec 仍 `text-pre` 纯文本 → 补齐详情页 Markdown 渲染，纯前端、零后端契约变更。最高优先级 1112-1116「Playwright验证-文档Story路径」为重复空描述 junk → 排除，新建高优先级增量 Epic 62 v4.9。
- **追踪（REST 新建）**：project 48(AUTODEV62)→epic 52(Epic 62 v4.9)→story 101(Story 62.1)→task 1117(high) → 合法链 `backlog→todo→in_progress→in_review`；story 101、epic 52 经 PATCH 同步 **in_review**（达成）。
- **实现（纯前端）**：
  - `app.html`：任务详情页 Description 卡与 Spec 卡由 `<div class="md text-pre">{{ current.description||'（空）' }}</div>` 改为 `@if (current.description) { <div class="task-md" [innerHTML]="renderMarkdown(...)"> } @else { <div class="task-md-empty">（空）</div> }`（Spec 同理）；复用既有 `renderMarkdown()`。
  - `app.css`：新增 `.task-md`（镜像 v4.8 `.qv-desc.md` 全部 Markdown 元素样式 + dark 适配）+ `.task-md-empty` 斜体占位。
  - 构建 `main-LMPCWHDD.js` / `styles-XJWX23MR.css`（全局样式未变）→ `cp` 至 `agentboard/web/static/`，删旧 `main-GDVLBW25.js`。
- **验证**：`tests/test_epic62_v49_task_desc_markdown_e2e.py` 全绿 —— 登录 admin→/task/{seed1}→Description 卡渲染 h1/strong/em/ul>li/code/a 且原始 `**` 标记消失；Spec 卡渲染 h2+列表；空描述任务 /task/{seed2} 显示「（空）」；**0** pageerror/console/.js+.css 404；种子任务测试末删除。回归 `pytest test_epic30_cache.py` 8 passed + E2E v4.8 抽屉 Markdown/v4.5 抽屉导航/v4.7 预设标签 全绿（无回归）。
- **提交**：`feat(ui): 前端小优化 - 任务详情页描述/Spec Markdown 渲染 (Epic 62 v4.9)` → push origin main。
- **硬约束**：未触碰 18001(MCP)/docker/端口（API 58125 / web 8090 / 8080 均原样）；排除 data/、autodev.lock、其他 automation 的 memory.md、screenshots、前端 dist 源码（仅提交 static 产物）。
- **下次可执行**：可将 Markdown 渲染推广到「Story/Epic 详情页描述」，或新需求。

## Epic 63 v5.0 Story/Epic 详情页描述 Markdown 渲染 → in_review（达成）
- **目标**：本次 task → in_review。MCP 连接器全断 → REST 兜底（API 58125 / web 8090，admin id=18）。
- **选型**：依上次「下次可执行」建议，将 Markdown 渲染推广到 Story/Epic 详情页（v4.8 抽屉、v4.9 任务详情页已渲染，仅 Story/Epic 详情页仍 `text-pre` 纯文本）→ 补齐，纯前端、零后端契约变更。最高优先级 1112-1116 为 junk、850-861 为整站级 admin-portal（1h 不可独立收尾）、708(medium,in_progress) 无描述且 scope 模糊（性能指标显示）→ 均排除，新建高优先级增量 Epic 63 v5.0。
- **追踪（REST 新建）**：project 49(AUTODEV63)→epic 53(Epic 63 v5.0)→story 102(Story 63.1)→task 1118(high) → 合法链 `backlog→todo→in_progress→in_review`；story 102、epic 53 经 PATCH 同步 **in_review**（达成）。
- **实现（纯前端，零新增 CSS）**：
  - `app.html`：Epic 详情页（`@case ('epic')`）与 Story 详情页（`@case ('story')`）描述块由 `text-pre` 改为 `@if/else` 三态——有描述 `<div class="card md task-md" [innerHTML]="renderMarkdown(current.description)">`，无描述 `.task-md-empty`「（空）」；Story 保留 `.story-description` 语义类。复用 v4.9 `.task-md`/`.task-md-empty`（含 dark），不新增样式。
  - 构建 `main-6IRL5C5X.js` / `styles-XJWX23MR.css` → `cp` 至 `agentboard/web/static/`，删旧 `main-LMPCWHDD.js`。
  - `openspec/changes/frontend-detail-markdown-v50/{proposal,design,tasks}.md` 已写。
- **验证**：`tests/test_epic63_v50_detail_md_e2e.py` 全绿 —— /story/102 渲染 h2/strong/em/ol>li/blockquote/code、/epic/53 渲染 h1/strong/em/ul>li/code/a，均无原始 `**`/``*斜体*`` 标记；**0** pageerror/console/.js+.css 404；种子用追踪实体 epic 53/story 102（已播 Markdown 描述）。回归 `pytest test_epic30_cache.py` 8 passed + E2E v4.9 任务详情 Markdown / v4.5 抽屉导航 全绿（无回归）。
- **提交**：`feat(ui): 前端小优化 - Story/Epic 详情页描述 Markdown 渲染 (Epic 63 v5.0)` → push origin main。
- **硬约束**：未触碰 18001(MCP)/docker/端口（API 58125 / web 8090 / 8080 均原样）；排除 data/、autodev.lock、其他 automation 的 memory.md、screenshots、前端 dist 源码（仅提交 static 产物）。
- **下次可执行**：Markdown 渲染体系已覆盖 抽屉/任务详情/Story 详情/Epic 详情 四处；可转向「批量指派面板增强」或新需求。

## Epic 64 v5.1 批量指派面板增强（成员头像/姓名 + 搜索）→ in_review（达成）
- **目标**：本次 task → in_review。MCP 连接器全断 → REST 兜底（API 58125 / web 8090，admin id=18）。
- **选型**：依 v5.0 运行「下次可执行」提示，将批量指派面板从原生 `<select>` 升级为成员头像/姓名 chip 选择器 + 搜索，与 v3.8 行内改指派菜单体验一致；纯前端、零后端契约变更。
- **追踪（REST 新建）**：project 50(AUTODEV64)→epic 54(Epic 64 v5.1)→story 103(Story 64.1)→task 1119(high) → 合法链 `backlog→todo→in_progress→in_review`；story 103、epic 54 经 PATCH 同步 **in_review**（达成）。
- **实现（纯前端）**：
  - `app.ts`：新增 `bulkAssignSearch` 信号 + `filteredBulkMembers()`（按 `username` 过滤 `members()`）；`showBulkActionPanel('assignee')`/`closeBulkActionPanel()` 重置搜索词。
  - `app.html`：`assignee` 面板由 `<select class="bulk-assignee-select">` 替换为搜索框 `.bulk-assign-search` + 成员列表 `.bulk-member-list`（「未指派」chip + 每成员 `.bulk-member-chip` 含 `.assignee-avatar-sm` 头像与姓名），点击成员 chip 即时 `bulkUpdateAssignee` 并收起面板、点击「未指派」即时清除。
  - `app.css`：删 `.bulk-assignee-select`，新增 `.bulk-assign-search`/`.bulk-panel--assignee`/`.bulk-member-list`/`.bulk-member-chip`（含 `.active` 高亮、hover 微交互）/`.bulk-member-empty`（复用 `--primary`/`--border`/`--text`，dark 自适应）。
  - 构建 `main-WEVKENIO.js`（app.css 为组件作用域，编译进 main）→ `cp` 至 `agentboard/web/static/`，删旧 `main-6IRL5C5X.js`。
  - `openspec/changes/frontend-bulk-assign-picker-v51/{proposal,design,tasks}.md` 已写。
- **验证**：`tests/test_epic64_v51_bulk_assign_picker_e2e.py` 全绿 —— 成员 chip 带头像（3 chip/3 avatar）、搜索 `qa1`→1、`zzzz`→无匹配提示、点击成员 chip 经 API 复核 assignee_id 变更、点击「未指派」复核清空、**0** pageerror/console/.js+.css 404；测试末清理种子任务与成员。回归 `pytest test_epic30_cache.py` 8 passed + E2E v5.0 详情 Markdown / v4.5 抽屉导航 全绿（无回归）。
- **提交**：`feat(ui): 前端小优化 - 批量指派面板增强（成员头像/姓名 + 搜索）(Epic 64 v5.1)` → push origin main。
- **硬约束**：未触碰 18001(MCP)/docker/端口；排除 data/、autodev.lock、其他 automation 的 memory.md、screenshots、前端 dist 源码（仅提交 static 产物 + 源码 + 测试 + openspec）。
- **下次可执行**：批量指派面板已与行内改指派体验统一；可转向「分组/排序维度持久化」或新需求（admin-portal 850-861 仍整站级高优先级 backlog）。
