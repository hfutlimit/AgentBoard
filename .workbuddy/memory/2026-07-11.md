# 2026-07-11 工作记录

## 自动开发周期（A-14 Markdown 编辑工具栏）
- 拉取最新代码：`git pull origin main` 已是最新（HEAD=faef13a）。
- 认领下一个 pending 项 **A-14 Markdown 编辑工具栏**（Epic 11 Backlog A）。
- 开发：新增 `mdToolbar(taName)`/`insertMd(ta,kind)`/`bindMdToolbar(scope)` 三个辅助；Task 详情编辑表单的 description/spec 文本框上方加「加粗/标题/列表/行内代码」按钮（行内类包裹选区、块级类在行首插入，含占位文本与自动选中），点击即插入 markdown 语法。`app.js`+43/−2、`style.css`+8（净增 ~49 行，符合 R2），未改 `models.py`/`api.py` 契约；工具栏样式随 CSS 变量适配深色模式。
- 部署：Docker Hub 仍不可达 → `docker compose up -d --build web` 基础镜像元数据解析失败；退化 `docker cp` 注入新 `app.js`/`style.css` 到运行中的 `agentboard-web-1`（/app/agentboard/web/static/）。HTTP 校验 app.js 含 `function mdToolbar`(1)、style.css 含 `md-toolbar`(1)、页面 200、`/api/meta` 200。
- 测试：托管 venv 跑 `tests/test_web_flow.py` + `tests/test_backend_flow.py` → **6 passed**，无回归。
- 推送：`git push origin main` 成功（`faef13a..d3a2bdd`，commit `d3a2bdd`）。
- 下一个 pending 项：**A-15 键盘快捷键**（j/k 上下移动选中项、e 编辑、Esc 关闭弹层）。

## 自动开发周期（Epic 11 前端持续优化）
- 补齐上一轮遗留未提交的 **A-02 状态色徽章**（Jira 风格 `.badge.status--<status>`），单独提交 `94f66ea`。
- 完成 **A-03 任务类型图标**：新增 `typeIcon()` 内联 SVG（task=勾选圆环 / bug=瓢虫），替换看板/列表/任务详情/类型徽章中的 emoji，不引入图标库。提交 `fd71dfd`。前端新增 23 行，符合迭代纪律。
- 部署：Docker Hub 拉取 `python:3.13-slim` 超时，退化用 `docker cp` 注入新静态文件到 `agentboard-web-1`，已验证线上 app.js 含 `typeIcon`。
- 测试：`test_web_flow.py` + `test_backend_flow.py` 共 **6 passed**，无回归。
- 已 `git push origin main`（`275cc9d..fd71dfd`）。

## 自动开发周期（续 · A-04 / A-05）
- **A-04 行内快速编辑标题**：列表项(Epic/Story/Task)标题与 Task 详情标题支持双击进入编辑态，回车/失焦 PATCH 保存、Esc 取消；列表项用单击导航/双击编辑计时区分。提交 `4e5658a`。
- **A-05 全局搜索框**：顶部栏加 `type=search` 输入框，新增 `applySearch()` 按标题实时过滤当前页列表（项目卡/实体列表/项目表/看板），空结果提示；查询词跨路由持久化，纯前端不改 API。改动 `index.html` +1、`app.js` +38、`style.css` +13（净增 ~52 行，符合 <~80 红线），未动 `models.py`/`api.py`。提交 `8e5c338`。
- 部署：基础镜像 `python:3.13-slim` 不在本地缓存 → `docker compose build` 会触发 Docker Hub 拉取超时；退化 `docker cp` 注入新静态文件到 `agentboard-web-1`（/app/agentboard/web/static/），HTTP 校验 app.js 含 `applySearch`(3)、index.html 含 `global-search`(1)。
- 测试：两轮均 **6 passed** 无回归；已 `git push origin main`（`4e5658a..8e5c338`）。
- 下一个 pending 项：**A-06 状态流转按钮组**（Jira 式状态工作流按钮，点击即 `PUT /api/tasks/{id}/status`）。

## 自动开发周期（续 · A-06 状态流转按钮组）
- **A-06 状态流转按钮组**：任务详情的「状态下拉 + 更新状态按钮」替换为 Jira 式工作流按钮组——当前状态药丸（`sf-current`）+ 合法迁移按钮（`sf-btn`，`STATUS_TRANSITIONS` 镜像后端 `service.TRANSITIONS`），点击即 `PUT /api/tasks/{id}/status`；后端仍为权威校验（非法迁移 400）。新增 `STATUS_TRANSITIONS` 常量与 `statusFlow()` 辅助；`app.js` +22/−7、`style.css` +17（净增 ~32 行，符合 <~80 红线），未动 `models.py`/`api.py`。提交 `6133905`。
- 部署：`python:3.13-slim` 仍不在本地缓存 → `docker compose build` 拉取超时；`docker cp` 注入新 `app.js`/`style.css` 到 `agentboard-web-1`，HTTP 校验 app.js 含 `statusFlow`(2)、style.css 含 `sf-btn`(10)。
- 测试：`test_web_flow.py` + `test_backend_flow.py` 共 **6 passed**，无回归。
- 已 `git push origin main`（`5359dfc..6133905`）。下一个 pending 项：**A-07 加载骨架屏**。

## 自动开发周期（续 · A-07 加载骨架屏）
- **A-07 加载骨架屏**：新增 `skeleton()` 占位（标题条 + 6 卡片网格 shimmer 动画 + 侧栏占位），`render()` 与 `index.html` 初始态均替换原「加载中…」文本 / 3-dot spinner，避免内容载入时的布局跳动。`app.js` +12、`style.css` +16、`index.html` +9（净增 ~35 行，符合 <~80 红线），未动 `models.py`/`api.py`。提交 `5040b96`。
- 部署：`python:3.13-slim` 仍不可达 → `docker cp` 注入新 `app.js`/`style.css`/`index.html` 到 `agentboard-web-1`，HTTP 校验 app.js 含 `function skeleton`、style.css 含 `sk-line`、index.html 含 `sk-grid`。
- 测试：`test_web_flow.py` + `test_backend_flow.py` 共 **6 passed**，无回归。
- 已 `git push origin main`（`2519dee..5040b96`）。下一个 pending 项：**A-08 空状态优化**。

## 自动开发周期（续 · A-08 空状态优化）
- **A-08 空状态优化**：新增统一 `emptyState(icon,title,desc,cta)` 辅助，将 Epic/Story/Task 列表空态由灰色「暂无」升级为「图标 + 引导文案 + 新建按钮」；CTA 通过 `document.getElementById(id).click()` 触发同页已有「＋ 新建」按钮。新增 `empty-compact` 紧凑型 CSS。`app.js` +19/−3、`style.css` +4（净增 ~23 行，符合 <~80 红线），未动 `models.py`/`api.py`。提交 `2b29ab7`。
- 部署：`python:3.13-slim` 元数据 TLS 超时（`docker compose build` 失败）→ `docker cp` 注入新 `app.js`/`style.css` 到 `agentboard-web-1`，HTTP 校验 app.js 含 `function emptyState`(1)、style.css 含 `empty-compact`(3)。
- 测试：`test_web_flow.py` + `test_backend_flow.py` 共 **6 passed**，无回归。
- 已 `git push origin main`（`5040b96..2b29ab7`）。下一个 pending 项：**A-09 进度条（Epic/Story）**。

## 自动开发周期（续 · A-09 进度条）
- **A-09 进度条（Epic/Story）**：新增 `progressBar(done,total)` 辅助（细条+百分比，颜色随完成度变化：100%绿/≥50%蓝/<50%灰，total=0 不显示）；`viewProject` 聚合每个 Epic 下所有 Story 的任务完成度（epics→stories→tasks）、`viewEpic` 计算每个 Story 的任务完成度（stories→tasks），卡片底部渲染进度条。`.entity-item` 加 `flex-wrap` 容纳换行进度条。`app.js` +30/−2、`style.css` +8（净增 ~38 行，符合 <~80 红线），未动 `models.py`/`api.py`。提交 `4e6df58`。
- 部署：`python:3.13-slim` 仍不可达 → `docker cp` 注入新 `app.js`/`style.css` 到 `agentboard-web-1`，HTTP 校验 app.js 含 `progressBar`(3)、style.css 含 `entity-progress`(1)、`/api/meta` 返回 200。
- 测试：`test_web_flow.py` + `test_backend_flow.py` 共 **6 passed**，无回归。
- 已 `git push origin main`（`2f1a4c0..4e6df58`）。下一个 pending 项：**A-10 深色模式开关**。

## 自动开发周期（续 · A-10 深色模式开关）
- **A-10 深色模式开关**：基于 CSS 变量切换明/暗主题——`[data-theme="dark"]` 覆盖 `--text/--bg/--card-bg/--border/--primary` 等变量，并对硬编码浅色表面（输入框/看板卡/分段按钮/幽灵按钮/代码行内）、hover 态、骨架屏占位条做兜底覆盖；顶栏 🌙/☀ 按钮点击切换，偏好存 `localStorage`（键 `agentboard_theme`）启动即应用。`app.js` +20、`style.css` +33、index.html +1（净增 ~54 行，符合 <~80 红线），未动 `models.py`/`api.py`。提交待 push。
- 部署：`python:3.13-slim` 仍不可达 → `docker cp` 注入新 `app.js`/`style.css`/`index.html` 到 `agentboard-web-1`，HTTP 校验 app.js 含 `toggleTheme`/`applyTheme`/`THEME_KEY`(8)、style.css 含 `data-theme="dark"`(22)、index.html 含 `theme-toggle`(1)。
- 测试：`test_web_flow.py` + `test_backend_flow.py` 共 **6 passed**，无回归。
- 已 `git push origin main`（`4e6df58..4b48974`，commit `4b48974`）。下一个 pending 项：**A-11 响应式布局**。

## 自动开发周期（续 · A-11 响应式布局）
- **A-11 响应式布局**：纯 `style.css` 增强——`≤768px` 时 `.layout` 转纵向、侧栏（树列表）堆叠为内容上方带 `max-height:42vh` 的可滚动面板（保留 ☰ 折叠）；按钮统一 `min-height:36px` 触摸目标、`.page-actions` 换行防溢出；`≤480px` 看板转 2 列、搜索框收窄。`style.css` +25/−3（净增 ~22 行，符合 <~80 红线），未动 `models.py`/`api.py`。提交 `84efd1d`。
- 部署：`python:3.13-slim` 仍不可达 → `docker cp` 注入新 `style.css` 到 `agentboard-web-1`，HTTP 校验 style.css 含 `A-11 响应式`(1)、`min-height: 36px`(1)、`/api/meta` 返回 200。
- 测试：`test_web_flow.py` + `test_backend_flow.py` 共 **6 passed**，无回归。
- 已 `git push origin main`（`dea5edaf..84efd1d`，commit `84efd1d`）。下一个 pending 项：**A-12 Toast 堆叠与动画**。

## 自动开发周期（续 · A-12 Toast 堆叠与动画）
- **A-12 Toast 堆叠与动画**：`#toast` 由单一覆盖元素改为多子项容器，每条提示独立 `.toast-item`（滑入淡入进场、停留 2.5s 后淡出移除，多条约提示互不覆盖）；支持可选 `type=error|success` 左侧色条（红/绿）。仅改 `app.js` +10（`toast()` 重写为 create/append/动画）、`style.css` +13（替换 `#toast` 块为容器+item 样式，净增 ~23 行，符合 <~80 红线），未动 `models.py`/`api.py` 契约，调用方向后兼容。
- 部署：**本次基础镜像本地缓存命中**，`docker compose up -d --build web` 规范重建成功（api/web 镜像重建、容器 recreated 且 healthy），无需退化 `docker cp`。HTTP 校验 `app.js` 含 `toast-item`(2)、`style.css` 含 `toast-item`(5)、`/api/meta` 返回 200、页面 200。
- 测试：`test_web_flow.py` + `test_backend_flow.py` 共 **6 passed**，无回归。
- 下一个 pending 项：**A-13 任务详情抽屉**（点击列表项从右侧滑出详情抽屉，不跳路由）。

## 环境踩坑（沙箱 · 续）
- `git pull origin main` 仍因 SSH 不可达失败（`Connection closed by 198.18.0.18 port 22`）；本地领先 origin，最终 `git push` 同样可能失败，需用户授权后本地重试。
- 与 07-10/11 早些轮次不同，本轮 `docker compose build` 基础镜像已缓存，重建可用，验证了"镜像缓存命中即可规范部署"的路径。

## 环境踩坑（沙箱）
- Windows venv 用 `Scripts\` 而非 `bin\`；Git Bash 路径用 `/c/Users/...` 不能用 `C:/...`。
- Docker Hub / 默认 PyPI 不可达 → 用 `docker cp` 注入 + 阿里云镜像 `mirrors.aliyun.com/pypi/simple` 装测试依赖（排除 fastmcp，3.13 下安装挂起）。

- 完成 **A-13 任务详情抽屉**：Story 页任务列表/看板项去 `href` 改用 `data-task-id`，单击右侧滑出抽屉（description/spec + 状态流转按钮，复用 `md()/statusFlow()`），遮罩/Esc 关闭并 `render()` 刷新列表；保留 A-04 双击编辑标题（200ms 计时区分）。index.html 加 `#drawer-overlay`/`#task-drawer`，style.css 加 `.drawer*` 样式含暗色兜底。提交 `d7d3a1c`，`docker compose up -d --build web` 重建成功，测试 6 passed，已 push。前端逻辑 <~80 行，未改契约。

## 自动开发周期（A-15 键盘快捷键）
- 拉取最新代码：`git pull origin main` 已是最新（HEAD=d3a2bdd）。
- 认领下一个 pending 项 **A-15 键盘快捷键**（Epic 11 Backlog A）。
- 开发：新增全局 `keydown` 监听（`j`/`k` 上下移动选中项、`e` 编辑选中项、`Esc` 关闭弹层由既有监听处理），复用 `inlineEditEnter()`/`route()`；`kbdItems()/kbdSet()/kbdEdit()` 在 `.entity-item/.project-card/.kanban-card` 中管理选中态（`.kbd-selected` 高亮 + `scrollIntoView`），输入框聚焦或带修饰键时跳过以免与输入冲突，`render()` 重置选中态；顶栏加 `⌨ j/k · e · Esc` 提示。`app.js`+41、style.css+8、index.html+1（净增 50 行，符合 R2），未改 `models.py`/`api.py` 契约。
- 部署：基础镜像 `python:3.13-slim` 仍不在本地缓存、`docker compose build` 拉取超时 → 退化 `docker cp` 注入新 `app.js`/`style.css`/`index.html` 到运行中的 `agentboard-web-1`（/app/agentboard/web/static/）。HTTP 校验 app.js 含 `function kbdItems`/`kbdSet`/`kbdEdit`(3)、style.css 含 `kbd-selected`/`kbd-hint`(6)、index.html 含 `kbd-hint`(1)、`/api/meta` 200、页面 200。
- 测试：托管 venv 跑 `tests/test_web_flow.py` + `tests/test_backend_flow.py` → **6 passed**，无回归。
- 下一个 pending 项：**A-16 复制链接**（任务/Story 提供「复制深链」按钮）。
