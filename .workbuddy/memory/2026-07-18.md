# 2026-07-18 AgentBoard 自动开发日志

## 05:00 自动开发周期（Epic 35 + Epic 36 → done）

### 本次交付
- **Epic 35（DB 25）前端体验升级 v1.5：任务关键词搜索 → done** ✅（commit `1f70841`，push 成功）
  - Story 35.1（DB 61）/ Task 904（DB 833）：任务列表工具条搜索输入框
  - 实现：`taskSearchQuery` signal + `.task-search-input`（180px→220px focus expand）+ `.task-search-clear` ✕ 按钮；`visibleTasks` computed 叠加 `title + description` 不区分大小写过滤。
  - 净增 58 行（app.ts +5 / app.html +14 / app.css +39），符合 R2 <80；不改后端契约。
  - Playwright E2E（`tests/test_epic35_search_e2e.py`）：搜索 156→1（精确匹配）、非匹配→0 + 空状态、✕ 清除恢复 156、清空输入恢复、零 page/console/.js+.css 错误。

- **Epic 36（DB 26）前端体验升级 v1.6：内联任务标题编辑 → done** ✅（commit `257c654`，push 成功）
  - Story 36.1（DB 62）/ Task 905（DB 834）：hover 显示 ✎ 编辑按钮 → 内联 input
  - 实现：`editingTaskId`/`editingTaskTitle` signals + `startInlineEdit()`/`saveInlineEdit()`/`cancelInlineEdit()`；`@if` 条件渲染 input vs span；Enter 保存、Esc 取消、blur 保存。
  - **关键坑**：Angular HttpClient 的 PATCH Observable 不 emit（原因不明，fetch 可正常 PATCH）→ `saveInlineEdit()` 改用 `fetch()` 直接调 API，绕过 HttpClient。同步清除编辑状态 + 异步后台保存，UI 即时响应。
  - `angular.json` 加 `"optimization": {"fonts": false}` 修复 Google Fonts @import 间歇性构建失败。
  - Playwright E2E（`tests/test_epic36_inline_edit_e2e.py`）：edit 按钮→input 显示正确值、Escape 关闭、Enter 保存关闭、零错误。
  - 回归 Epic 35 E2E 仍 PASS。

### 技术发现
- Angular HttpClient PATCH 在本项目环境中不返回响应（request 发出但 response 不触发 Observable emit）；GET/PUT 均正常。改用 `fetch()` 作为 workaround。
- `web_app.py` SPA fallback 有效：`page.goto(f"{BASE}/story/33")` 可直接触发 Angular Router 的 `loadRoute()`（无需 hash 导航或侧栏点击）。
- Playwright `fill()` 会触发 Angular `(input)` 事件，signal 正常更新。

---

## 02:00 自动开发周期（Epic 34 → done）

### 本次交付
- **Epic 34（DB 24）前端体验升级 v1.4 → done** ✅（commit `4e7b4f2`，push 成功 `af9ddb7..4e7b4f2`）
  - Story 34.1（DB 60）/ Task 903（DB 831）：任务列表汇总栏
  - 实现：`taskListSummary()` computed 基于 `tasks()` 聚合 {total, done, inProgress, rate, segments}；`app.html` 在 entity-list 上方插 `.task-list-summary`（状态分布堆叠条 + "共 N 项 · 已完成 X · 进行中 Y · 完成率 Z%"）；`app.css` 堆叠条/文案样式，复用 `--status-*-bg` 段色 + `--surface-2/--border-2/--text-2`。
  - 仅列表模式显示（`@if (!boardMode())` + `@if (tasks().length > 0)`），看板模式隐藏。
  - 净增 57 行（app.ts +12 / app.html +12 / app.css +33），符合 R2 <80；不改 `models.py`/`api.py` 契约。

### 验证
- **Playwright E2E**（`tests/test_epic34_summary_e2e.py`，本地 8080 → 58125 agentboard.db）：
  - `.task-list-summary` 渲染 1 个、`.summary-seg` 3 段、文案含 共/完成率
  - **一致性校验**：summary total(153) == task list rows(153) ✅（汇总栏与任务列表 `tasks().length` 一致）
  - 切换 看板→列表：摘要消失（kanban=0）→ 重现（list=1）✅
  - 零 pageerror / 零 console error / 零 .js+.css failed request ✅
- **回归**：Epic 33 E2E（`test_epic33_e2e_v2.py`）仍 PASS（19 进度条、零错误）。
- **后端回归**：`pytest test_task_estimate.py test_labels_api.py` 测试套件挂起（4+ 分钟无输出，疑似 DB fixture 慢），已停止；本次改动纯前端，不涉及后端，E2E 回归已覆盖。

### 关键发现 / 踩坑
- **`tasks()` 信号在直接 `page.goto(/story/N)` 时含全项目任务（153）而非 story 级（6）**：根因是 `loadDashboard()`（app.ts L891-905，dashboard 路由会 `Promise.all(allStories.map(s => listTasks(s.id)))` 加载全量 tasks）与 story 路由的 `tasks.set(storyTasks)` 存在竞态——首次 `loadRoute()` 在路由解析前看到 `/` 触发 `loadDashboard()`（慢，47 个 listTasks 请求），路由解析到 `/story/N` 后第二次 `loadRoute()` 设 `tasks.set(6)`，但 `loadDashboard()` 随后完成 `tasks.set(153)` 覆盖。**属既有 SPA 路由竞态，非本次改动引入**；汇总栏与任务列表 `{{ tasks().length }}` 始终一致。修复需重构 `loadRoute` 的 dashboard 预加载守卫，超出 R2 单次小优化范围，记录为后续项。
- **Playwright failed-request 过滤约定**：既有 E2E（`test_epic33_e2e_v2.py` L22）只计 `.js`/`.css` 失败请求；`/api/*` 的 `ERR_ABORTED`（导航中断 in-flight 请求）是既有良性现象。新 E2E 沿用此约定，否则会把 dashboard 预加载竞态的 47 个 ERR_ABORTED 误判为失败。
- **managed python venv**：`C:\Users\jason\.workbuddy\binaries\python\envs\default\Scripts\python.exe` 已装 playwright 1.61.0，`from playwright.async_api import async_playwright` 可用（注意 `import playwright; playwright.__version__` 会 AttributeError，用 async_api 子模块验证）。
- **前端构建部署**：`npm run build`（managed node 22.22.2）→ `frontend/dist/frontend/browser/` → cp 到 `agentboard/web/static/`；本地 web 8080 与 Docker web 28080 都挂载/读取该目录，**无需 docker rebuild**。`web_app.py` 注入 `window.AGENTBOARD_API='http://127.0.0.1:58125'`（本地）。
- **端口保护**：全程未触碰 18001（WorkBuddy MCP）；API 58125 / Web 8080 / Docker 28080 保持原样。

### MCP 状态流转
- Epic 24（34）：backlog → in_progress → in_review → done
- Story 60（34.1）：backlog → in_progress → in_review → done
- Task 831（903）：backlog → todo → in_progress → in_review → done
- 全部通过 REST API（curl）完成（MCP auth 仍不可用，沿用既有绕过方案）。

### 下次建议
- 修复 `loadRoute()` dashboard 预加载竞态（`tasks()` 在 story 页被全量覆盖）——加 `if (kind)` 守卫跳过 dashboard 预加载，或在 story/epic 路由开始时取消 in-flight `loadDashboard`。
- 清理 project 3 残留的 2 个测试副本任务（task 829/830，"Task 902: ... (副本)"）。
- 继续 Epic 11 Backlog：B-02 负责人指派（需用户列表端点）/ B-06 按负责人分组（依赖 B-02）；或新 UI 小优化（命令面板 Ctrl+K、侧栏项目任务计数徽章等）。

---

## 08:00 自动开发周期（B-02 负责人指派 → done）

### 本次交付
- **B-02 负责人 / 指派（assignee）→ done** ✅（Epic 37 / Story 63 / Task 835；REST 状态流转 in_progress→in_review→done 完成）
  - 前端（纯前端接入，后端 `assignee_id` 字段/API 已就绪）：
    - 创建弹窗 `#create-assignee` 成员下拉；任务详情编辑表单 `#task-assignee` 成员下拉；看板卡片 `.assignee-chip` 负责人头像。
    - `saveTaskAssignee()` 走 `fetch()`（绕过 Angular HttpClient PATCH 不返回问题），toast「负责人已更新」。
    - **关键修复**：`loadRoute()` 的 `story` 分支与 `task` 分支原先**不调用 `loadMembers`**，导致 story/task 视图下 `members()` 为空、负责人下拉无候选 → 在 story/task 分支补 `await this.loadMembers(epic.project_id / project.id)`（修复位置 `app.ts` L759-762 附近）。
  - 后端 **真实 bug 修复**（顺带）：`api.py` `add_member` 端点 `user_id = body.get("user_id") or (service.get_user_by_username(...) or {}).get("id")` —— `get_user_by_username` 返回 `User` ORM 对象而非 dict，`.get("id")` 触发 `AttributeError` → 500。改为先取 `found_user` 再取 `.id`。

### 验证
- **Playwright E2E**（`tests/test_b02_assignee_e2e.py`，fixture 自建 temp DB + uvicorn api/web）：注册双用户 A/B → B 加入项目成员（验证 add_member 修复，返回 201）→ 创建任务指派给 B → 详情「负责人」显示 B 且 API `assignee_id==B.id` → 详情改派给 A，toast「负责人已更新」+ API 确认 → 看板 `.assignee-chip` 显示 → **零 pageerror / 零 console error / 零 .js+.css failed**。✅ PASS（60s）
- **回归**：`test_story_151_notifications.py` / `test_api_keys.py` / `test_due_date.py` / `test_b02_assignee_e2e.py` 共 7 项全绿。失败项全部为**环境/历史问题**：`test_crud_smoke.py`(9) 与 `test_backend_flow.py`(2) 命中运行中的 Docker API `:8000`（本会话未起，ConnectError）；`test_labels_api.py` `assert 201==200`（创建返回 201 Created，历史断言缺陷，与本次改动无关）。

### 关键发现 / 踩坑
- **`<option>` 元素在 select 未展开时被 Playwright 判为 hidden**：`wait_for_selector(option, state="visible")` 永不就绪 → 改用 `state="attached"`。
- **任务链接选择器**：任务列表项结构是 `div.entity-item > a.entity-item-main`（href=`/task/N`），而 epic/story 列表项是 `a.entity-item`。E2E 打开任务详情改用「API 取 task id → `page.goto(/task/{id})`」直跳，避免依赖列表渲染时机/选择器。
- **`/api/stories/{id}` 序列化不含 `project_id`**（Story 模型只有 `epic_id`）：E2E 改用创建项目时已解析的 `pid` 取 `project_id`。
- **PATCH 偶发 ReadTimeout / 404（既有连接重置现象）**：status 流转时 task/epic PATCH 偶有超时，重试/稍后 GET 确认已生效；story PATCH 首次 RETRY_FAIL 后单次直发即 200。属本机 58125 服务连接重置，非代码问题。
- **端口保护**：全程未触碰 18001（MCP）；58125 / 18000 仅做 REST 状态查询与流转。

### 提交
- 计划 commit `feat(ui): 前端小优化 - B-02 负责人指派` → `git push origin main`（含 `agentboard/api.py` 后端 bug 修复 + `frontend/src/app/app.ts` 成员加载修复 + 部署 `agentboard/web/static/` + `tests/test_b02_assignee_e2e.py` + `docs/tasks.md`）。
- 删 `.workbuddy/autodev.lock`。

### 下次建议
- B-06「按负责人分组」依赖本任务的成员体系（已完成），可作为下一轮子项。
- 修复 `loadRoute()` dashboard 预加载竞态（`tasks()` 在 story 页被全量覆盖）仍为待办。
- 清理 `test_labels_api.py` 的 `assert 201==200` 历史断言缺陷（应接受 200/201）。
