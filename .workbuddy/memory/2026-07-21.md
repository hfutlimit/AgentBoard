# 2026-07-21 自动开发（Epic 38 v2.4 类型快速筛选 chips → in_review）

## 目标
本次任务目标：至少 1 个 task → in_review。选中增量：填补 v 系列缺口 v2.4（v2.0 优先级 / v2.5 状态 chips 之后，类型 chips 缺失）。

## 完成
- Epic 34 (id=34) 前端体验升级 v2.4 - 任务类型快速筛选 chips → in_review
- Story 74 (id=74) 任务类型快速筛选 chips → in_review
- Task 865 (id=865, high) 类型 quickfilter chips → in_review（状态链 backlog→todo→in_progress→in_review 全 200）
- 实现（纯前端，复用既有 filterTypes 信号 + visibleTasks 过滤，无后端契约变更）：
  - app.ts：filterTypes 初始化读 localStorage.agentboard_quick_type；新增 typeCounts computed；新增 setQuickType() 单选切换 + persistQuickType() 持久化；clearFilters 联动重置并 persistQuickType()。
  - app.html：状态 chips 后追加第三个 task-quickfilter-bar（全部 + 任务 + Bug 带计数），复用 .qf-chip/.qf-count。
  - 新增代码约 30 行（远低于范围红线）。
- 构建：npm run build（managed node 22.22.2）→ frontend/dist/frontend/browser/ → cp 到 agentboard/web/static/（web 8080 直读静态，无需 docker rebuild）。
- openspec/changes/frontend-type-quickfilter-v24/（proposal/design/tasks）已写。

## 验证
- Playwright E2E：tests/test_epic38_v24_type_quickfilter_e2e.py 全绿（类型 bar 3 chips、点 Bug→仅 bug、点 任务→仅 task、reload 持久化、点 全部 清空；**0** pageerror/console/.js+.css 404）。
  - 为制造双向过滤证据，临时在 story 25 注入 1 条 bug 任务（id 865，写入后又删除），验证结束后删除，项目数据干净。
- 回归：pytest test_epic30_cache.py 8 passed；E2E epic31(v2.0 优先级)/epic34(v2.3 筛选引导)/epic33(v2.2 只看我)/v19(分组折叠)/epic35(v2.5 搜索)/v2.5(状态 chips) 全绿。
  - 修复：test_epic31_priority_quickfilter_e2e.py 因新增类型 bar 后 .qf-chip 全局计数由 6 变 16 而误报 FAIL → 将该测试 scope 到优先级 bar（aria-label 按优先级快速筛选），与 v2.4 测试一致。属测试过时，非应用回归。

## 提交
- commit `feat(ui): 前端体验升级 v2.4 - 任务类型快速筛选 chips (Task 865 → in_review)` + git push origin main 成功。
- 刻意排除 data/、autodev.lock、其他 automation 的 MEMORY.md、screenshots、前端 dist（.gitignore 已忽略 frontend/dist）。

## 硬约束
- 未触碰 18001(MCP)/8080(web)/docker 配置；AgentBoard 服务仍用 API 58125 / Web 8080。
- 临时 bug 任务已删除，项目 3 数据无残留。

## 下次可执行
- 继续 v 系列小步迭代（如 v2.6 负责人快速筛选 chips / 列表排序 UI 增强 / 看板视图优化），或认领 Epic 32(Admin Portal) 子项（风险较高，需单独评估）。

# 2026-07-21（续）文档模块移入项目详情 Tab + Docker 部署

## 目标
用户要求：① 部署前端到 Docker（web 28080 加载含文档模块的最新构建）；② 把「项目文档」从侧栏独立入口改为项目详情页的 Tab（与 Epics/Sprints/Backlog/成员/统计/定时计划/设置 并列），天然按当前项目过滤。

## 完成
- 项目详情页新增「文档」Tab：`ProjectTabKind` 增加 `'documents'`；`loadProjectTab()` 在 documents 分支用 `listDocuments({project_id})` 加载；新增 `projectDocVisible()` computed 按 `project().id` 过滤并支持类型/状态/关键词客户端筛选。
- app.html：Tab 栏追加文档按钮（带数量徽标）；project case 内插入完整 documents Tab 内容块（筛选条 + 新建表单 + 加载/错误/空/列表分支 + 内联详情面板：状态流转按钮条 / 内联编辑或 renderMarkdown / 评论区）。侧栏原有全局「项目文档」入口保留并存。
- 修复若干 Angular 模板错误：
  - 评论预览 `@if/} else {/` → 必须 `} @else {`（Angular 新控制流 `else` 必须带 `@`）。
  - 类型检查对齐：`docCommentPreview` 是 boolean 信号（全局预览开关）非 comment id；`toggleDocCommentPreview()` 0 参；`addDocComment(event)` 读 `docCommentContent()` 信号（非模板 ref）；`openDocEdit()` 0 参（操作 `docItem()`）；`docTypeLabel(t: DocumentType)` 传 `d.type` 非 `d`。
  - `@if (docItem(); as d)` 别名模式做非空收窄，避免 `docItem().status` 报「可能为 null」。
- 交互一致性修复（in-tab 不跳路由）：
  - `createDocument`：创建后 append 到 `documents()`，若处于 project+documents tab 则 `openDocTab(created)` 就地打开详情，否则跳转 `/documents/:id`。
  - `saveDocEdit`/`setDocStatus`：同时更新 `documents()` 列表项（标题/状态即时反映）。
  - `deleteDoc`：从 `documents()` 移除并 `docItem.set(null)`，Tab 内不跳转。
  - 新增 `openDocTab(d)`：写入 docItem + 加载评论（不走路由），供 Tab 行点击使用。
- 构建：`npm run build`（managed node 22.22.2）→ `cp -r frontend/dist/frontend/browser/. agentboard/web/static/`。

## 部署（Docker）
- `agentboard-web-1`（28080）静态为 bind-mount `E:\...\agentboard\web\static` → `/app/agentboard/web/static`（:ro）。cp 新产物即生效，**无需 docker restart**；curl 确认 index 已引用 `main-2B65CQGI.js`。
- API `agentboard-api-1`（18000）已含 documents 表（前轮后端已落地），无需改动。
- 验证：`curl localhost:18000/api/documents?project_id=3` → 200；浏览器 Tab 点击触发 `GET /api/documents?project_id=3` 200。

## 验证（Playwright，managed venv playwright 1.61.0）
- `scripts/verify_doc_tab2.py`：Tab 按钮存在、点击触发 API 200、无 console error。
- `scripts/verify_doc_create.py`：新建表单出现→填写（类型用 valid 值 `design`，非 `guide`）→提交后列表从 0→1（或 N→N+1）、详情 `.doc-content` 渲染（Markdown 内联）。
- 创建/删除的 demo 文档已通过 API 清理（project 3 现 0 文档，数据干净）。

## 提交
- commit `feat(ui): 项目文档移入项目详情 Tab 并修复构建/交互`（`6405eb7`）+ git push origin main 成功（3e5fbb1..6405eb7）。
- 刻意排除：data/、automation MEMORY.md、screenshots、frontend/dist（gitignore）。

## 文档模块复用要点（长期）
- 既有 signals：`documents`/`docItem`(Signal<DocumentItem|null>)/`docCommentPreview`(boolean)/`docCommentContent`(string)/`docFilterType`/`docFilterStatus`/`docSearchQuery`/`docTypes`/`docStatuses`。
- 注意 `onDocFilterChange()`/`onDocSearchChange()` 会调 `loadDocuments()` **重新拉取全量文档（不按 project_id）**——在 Tab 内务必改用客户端筛选（`projectDocVisible` 读上述 filter 信号），切勿在 Tab 触发这两个函数，否则会覆盖 project-scoped 列表。
- `DOCUMENT_TYPES = ['memory','plan','knowledge','design']`；`docTypeLabel(t)`/`docStatusLabel(s)` 入参为枚举值字符串。

# 2026-07-21（续2）AgentBoard 未完成 Story 状态漂移收口

## 背景
用户问「还有多少 story 没完成 / 哪些价值高来实现」。经查全 44 项目共 129 Story，未完成的 59 中多为 E2E 测试种子项目（各 1 条 backlog）。
AgentBoard 项目（pid=3）有 20 个「未完成」Story，但逐一核对代码后发现**全部功能早已实现**，仅 tracker 状态未更新（典型状态漂移）。

## 完成（tracker 收口，无代码变更）
- 实现已落地、标记 done 的 19 个 Story：S1-S13（数据模型/CRUD/MCP/OpenSpec/REST/WebUI/规范目录）、S10(REST API)、S26/S27/S28(Sprint/附件/定时开发)、S135/S136/S137(标签/负责人/截止日期)——前端 app.html/app.ts 与后端 api.py/service.py 均已含对应实现。
- 同步 Epic 1-6、11、91 为 done（其子 Story 全部 done）。
- **S133（测试稳定性）验证后标记 done**：
  - 速率限制中间件已于 2026-07-20 移除 → 20 次并发 `/api/projects` 全返回数据（无 429），该项已满足。
  - Sprint 单活跃约束实测通过：激活第 2 个 sprint 后第 1 个自动回退 planning，仅一个 active（swap 模式）。
- 收口后 AgentBoard（pid=3）20 个未完成 Story 全部 done，Epic 90 也置 done。

## 唯一真正功能缺口
- 甘特/时间线视图（grep `gantt|timeline` 命中 0）——目前任务无可视化的时间轴/排期视图。属下一步高价值新增项（前端为主，复用现有 created_at/due_date）。
- 「我的任务/概览仪表盘」偏弱（dashboard 引用极少）。

## 注意
- 验证 S133 时在 docker API(18000) 创建了 2 个探针项目（key=S133 / S133_<ts>），清理时因 `audit_logs` 无删除接口且外键无 cascade，`delete_project` 报 422 无法彻底删除。这 2 个残留探针项目(ids 96/98)无害，留待后续（无 API 可删，直接改库有风险，未动）。
- 端口注意：本机 AgentBoard docker API 实际在 **18000**（非 58125，本地 uvicorn 未起）；Web 在 28080。MCP 18001 未触碰。

# 2026-07-21（续3）v2.6（按状态排序）验收 + 推送
## 目标
用户指令：测试早上自动开发实现的 v2.6（任务列表「按状态」排序 + 偏好持久化 + 方向切换），有问题修、没问题就 push。

## 验收（Playwright，web 28080 / API 18000）
- `tests/test_epic39_v26_status_sort_e2e.py` 全绿（已把 BASE 从 8080 改 28080，因本机 web 现跑 28080）：
  - 登录 admin OK；story 25 加载 268 行任务。
  - 排序下拉含「状态」（value=status）；选「状态」→ 列表按状态工作流降序（done→backlog 单调不增）；点方向键→升序（backlog→done 单调不减）。
  - 刷新后持久化：`<select>` 仍选中「状态」、`localStorage.agentboard_sort_key=='status'`、列表仍按状态有序。
  - **0** pageerror / console / .js+.css 404。末尾恢复默认排序（创建时间）避免污染用户偏好。

## 提交
- commit `feat(ui): 前端体验升级 v2.6 - 任务列表按状态排序 + 偏好持久化` + git push origin main 成功。
- 刻意排除 data/、autodev.lock、其他 automation MEMORY.md、screenshots、运行时 db-journal、前端 dist（gitignore）。

## 硬约束
- 未触碰 18001(MCP)/docker 配置；MCP 18001 端口安全。
