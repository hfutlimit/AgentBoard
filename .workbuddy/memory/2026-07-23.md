# 2026-07-23 自动开发日志

## Epic 42 v3.0 任务列表批量指派（达成 task → in_review）
- **目标**: 本次 task → in_review。MCP 连接器本次可用（agentboard-mcp-1:18001 经 WorkBuddy 桥接，admin 登录成功）；MCP 的 MariaDB 仅含 5 个原始项目 + project 3 的近期追踪 epic（66-69 in_review），本地 SQLite(58125) 有 36 个项目，两者数据不完全一致 → 按"MCP 优先"在 MCP 建追踪实体。
- **选型**: MCP backlog 大 epic（15 文档维护 / 64 腾讯云 COS）1 小时内无法收尾 → 新建增量 Epic 42 v3.0 补齐批量第 4 操作「批量指派」（bulk 状态/优先级/删除已有）。
- **MCP 状态流转**: 新建 Epic 71 → Story 74 → Task 740(high) → backlog→todo→in_progress→in_review；Story 74 / Epic 71 同步 in_review（达成）。
- **实现（后端增量 + 前端）**:
  - `agentboard/api.py`: `BulkTaskUpdate` 加 `assignee_id: int|None=None` + `clear_assignee: bool=False`；`bulk_update_tasks` 增加分支（assignee_id 优先，否则 clear_assignee→None）。service.update_task 已支持 assignee_id，零契约破坏。
  - `frontend/src/app/api.service.ts`: `bulkUpdateTasks` 类型扩展 {assignee_id?, clear_assignee?}。
  - `app.ts`: 新增 `bulkAssigneeId` 信号 + `bulkUpdateAssignee()`（镜像 bulkUpdatePriority）；`showBulkActionPanel` 类型扩 'assignee'。
  - `app.html`: 批量栏加「批量指派」按钮 + 指派人面板（成员 select + 未指派 + 应用）。
  - `app.css`: `.bulk-assignee-select`。
- **构建**: `npm run build`(node22.22.2, 清 .angular/cache) → main-KJONYJDT.js；cp dist/frontend/browser/. → agentboard/web/static/，删旧 main。
- **验证**:
  - 后端 pytest `tests/test_epic42_bulk_assign.py` 3 passed（assignee/clear/legacy 回归）。
  - 前端 Playwright `tests/test_epic42_bulk_assign_e2e.py` 全绿（勾选3未指派任务→批量指派 ticket_creator(uid=2)→API 校验 3 任务 assignee_id=2；再批量清除→None；**0** pageerror/console/.js+.css 404；还原现场）。
  - 回归：`pytest test_epic30_cache.py` 8 passed；`test_bulk_priority_e2e.py` 全绿（0 错误）。
- **提交**: feat(ui): 前端小优化 - 任务列表批量指派 (Epic 42 v3.0) + push origin main。
- **硬约束**: 未触碰 18001(MCP)/docker 配置；排除 data/、autodev.lock、其他 automation 的 memory.md、screenshots、前端 dist 源码目录（仅提交 web/static 产物）。
- **下次可执行**: bulk 四件套齐（状态/优先级/指派/删除）；可转向「保存筛选预设」「批量改截止日期」或新需求。

## Epic 43 v3.1 任务列表筛选预设（达成 task → in_review）
- **目标**: 本次 task → in_review。MCP 连接器断开（agentboard 全 disconnect）→ REST 兜底（本地 uvicorn 58125 为权威）。
- **选型**: v 系列 chips 家族（priority/status/type/assignee/due）+ bulk（状态/优先级/指派/删除）已齐；「仅看我」已有。最高优先级可独立交付的增量 = **筛选预设**（保存/应用/删除当前筛选组合）。纯前端 localStorage，零后端契约变更。
- **MCP/REST 追踪实体**: 新建 project 38 → epic 47 → story 96 → task 1106(high) → 状态链 backlog→todo→in_progress→in_review；story 96 / epic 47 同步 in_review（达成）。
- **实现（纯前端 ~70 行）**:
  - `app.ts`: 新增 `FilterPreset` 接口 + `filterPresets`/`presetName`/`presetOpen` 信号 + `loadFilterPresets()/persistFilterPresets()`；新增 `saveFilterPreset()`（快照 5 维度 chips + 搜索 + 只看我）、`applyFilterPreset(i)`（先 `clearAllFilters()` 再逐项 `setQuick*()`/`taskSearchQuery.set()`/`filterMineOnly.set()` 还原）、`deleteFilterPreset(i)`、`togglePresetOpen()`。复用既有 setter，避免状态不一致。
  - `app.html`: `task-toolbar-secondary` 内加「📑 预设」按钮（数量徽标）+ 浮层（保存输入 / 预设列表 / 应用 / 删除）。
  - `app.css`: `.preset-wrap/.preset-panel/.preset-item/.preset-apply/.preset-del` 等（复用 .ghost-sm/.btn-primary-sm 视觉）。
- **关键发现（重要）**: commit `2aa4155 feat(ui): 精简 Story 任务筛选条` 已把优先级/类型/截止日期 3 个快速筛选 chips 条**收进高级筛选面板**，现任务列表工具条**仅剩「状态」+「指派人」2 个常驻 chips 条**（`.task-quickfilter-bar` 现仅 2 个）。旧 E2E `test_epic40_v28_due_quickfilter_e2e.py` 引用「无截止」due 条 → **已失效/过时**，非本次改动导致。
- **构建**: `npm run build`(node22.22.2, NODE_OPTIONS=4096, 清 .angular/cache) → main-ZDJNSU6T.js；cp dist/frontend/browser/. → agentboard/web/static/，删旧 main-KJONYJDT.js。
- **验证**: Playwright `tests/test_epic43_filter_presets_e2e.py` 全绿 —— 登录 admin→/story/25→组合「状态=完成 + 指派人=未指派」→保存 P1（徽标=1）→清除（行数回总 8）→应用（两 chip 高亮、行数=交集 6）→刷新持久化（预设仍在、行数=6）→删除（面板清空、徽标=0）；**0** pageerror/console/.js+.css 404。
- **回归**: 后端 `pytest test_epic30_cache.py` 8 passed（无后端改动，零回归）；前端 `test_epic39_v27_assignee_quickfilter_e2e.py` 失败为「隔离 story 注入任务 0 行」的测试自身数据/时序问题（与本次纯前端新增无关，story 25 列表与 chips 经 v3.1 E2E 验证正常渲染）；`test_epic40_v28_due_quickfilter_e2e.py` 失败为 due 条已移除（过时测试）。两项均为预先存在/过时，非本次回归。
- **提交**: `feat(ui): 前端小优化 - 任务列表筛选预设 (Epic 43 v3.1)` + push origin main。
- **硬约束**: 未触碰 18001(MCP)/docker 配置；排除 data/、autodev.lock、其他 automation 的 memory.md、screenshots、前端 dist 源码目录（仅提交 web/static 产物）。
- **待清理(非阻塞)**: 误建空追踪 project 37「v31-track」无法删除——`delete_project` 不级联 `project_members` 致 FK 约束冲突（后端既有局限，超出本次前端任务范围），留作无害空项目。
- **下次可执行**: 可转向「批量改截止日期」「保存筛选预设服务端共享」或新需求；注意 v2.8/v2.0/v2.4 旧 E2E 因精简筛选条已过时，需迁移断言到高级面板或状态/指派人 chips。

## 任务详情页移除内联编辑表单，改为弹窗编辑
- **问题**: 任务详情页（`/task/:id`）仍展示完整的内联「编辑任务」表单，与项目统一的弹窗编辑 UX 不一致。
- **实现**:
  - `frontend/src/app/app.ts`: 新增 `taskEditModal` + 8 个编辑字段信号；新增 `openTaskEditModal()`/`closeTaskEditModal()`/`submitTaskEditModal()`；删除已废弃的 `scrollToEdit()`。
  - `frontend/src/app/app.html`: 移除详情页内联表单（含标题/类型/优先级/截止日期/标签/Sprint/负责人/Description/Spec）；`quick-actions` 的「编辑」改为 `openTaskEditModal(current)`，并把原表单中的「生成子任务」移入 quick-actions；新增 `taskEditModal` 弹窗，统一收集全部字段后一次性 PATCH。
- **构建**: `npm run build`(node22.22.2) → main-BGS3FFRS.js；cp dist/frontend/browser/. → agentboard/web/static/，删旧 main-ZDJNSU6T.js。
- **提交**: `feat(ui): 任务详情页移除内联编辑表单，改为弹窗编辑` + push origin main。

## 部署到 Docker（前端热挂载）
- 本次仅改前端静态；`docker-compose.yml` 的 `web` 服务 bind-mount `./agentboard/web/static:/app/agentboard/web/static:ro` → 静态文件实时生效，**无需 rebuild 镜像**。
- 重启 `agentboard-web-1`（端口 28080）后验证：index.html 引用新 `main-BGS3FFRS.js`（200），旧 `main-ZDJNSU6T.js` 已删（缺失路径走 SPA fallback 返回 index.html，故 curl 旧 hash 得 200 是假阳性）。
- 全栈健康：web-1(28080 healthy)、api-1(18000 healthy, /api/meta 200)、db-1、mcp-1 均 Up。
- **踩坑记录**: `agentboard-web-new`（端口 18080）实为**误标容器**——其 command 是 `uvicorn agentboard.api:app --port 8000`（API 服务），端口映射 18080→8080 与内部 8000 不匹配 → curl 18080 全 000。它并非前端 web，属历史残留，与本次部署无关，勿将其当作前端实例。

## 设计原型专家团：AgentBoard 界面优化（编辑按钮太丑）
- 走标准 SOP 团队 `design-engine-agentboard-ui`：需求发现→设计系统→原型→审查→导出→Story。
- **Phase1 审计**：截图目录 `agentboard-audit/screenshots/`（14 张）；核心问题=Epic/Story 详情「▸ 编辑 X」disclosure 文字按钮丑 + Task 详情 5 按钮层级混乱 + 文档弱文本「编辑」链接。
- **Phase2 设计令牌**：选 Linear(基底)+Stripe(按钮层级)+沿用主色 #4F46E5；四档按钮 Primary/Secondary/Ghost/Danger-ghost；令牌文件 `C:\Users\jason\.workbuddy\teams\design-engine-agentboard-ui\design-tokens.css|json|md`。
- **Phase3 原型**：4 个自包含 HTML 存 `docs/design-prototypes/`：`epic-detail.html` `story-detail.html` `task-detail.html` `doc-detail.html`（令牌内联、内联 SVG、响应式）。
- **Phase4 审查**：5 维 + Anti-Slop，4/4 PASS（P0=0）；P1=tertiary 对比度 #94A3B8→#64748B（已修正到 4 文件与令牌）。
- **Phase5 导出**：用 Playwright(managed venv) 渲染 4 张 PNG 到同目录：`epic-detail.png` `story-detail.png` `task-detail.png` `doc-detail.png`（1440x900 @2x 全页）。
- **Phase6 Story**：在 AgentBoard 项目 3 / Epic 89「用户体验持续优化」下创建 **Story #181**「统一全局操作按钮视觉规范（修复丑陋的「编辑」控件）」，status=backlog，描述含 4 张原型图绝对路径 + 设计令牌路径 + 改动点 + 验收标准，供后续自动化任务实现。API=REST curl（token 字段为 `token` 非 `access_token`；项目列表 key=`items`；Story 创建 `POST /api/epics/89/stories` body `{title,description}`）。

## 21:46 自动开发 — Epic 45 v3.2 批量改截止日期 → in_review（达成）
- **目标**：本次 task → in_review。MCP 连接器全部断开 → REST 兜底（Docker API 18000 / web 28080）。
- **选型**：bulk 四件套（status/priority/assignee/delete）已齐，缺「截止日期」→ 补齐批量第 5 项（纯后端增量字段 + 前端面板），零契约破坏。
- **追踪**：REST 新建 project 107(AUTODEV45)→epic 115(Epic 45 v3.2)→story 182→task 894(high) → 经合法链 `backlog→todo→in_progress→in_review` 置 **in_review**；story 182、epic 115 同步 **in_review**（达成）。
- **实现**：
  - 后端 `agentboard/api.py`：`BulkTaskUpdate` 新增 `due_date:str|None` + `clear_due_date:bool`；`bulk_update_tasks` 校验后写入（clear 优先，None 可清空，因 `update_task` 的 nullable_fields 含 due_date）；`service.update_task` 已支持 due_date（无需改）。
  - 前端：`api.service.ts` 签名补 `due_date?/clear_due_date?`；`app.ts` 新增 `bulkUpdateDueDate()`（镜像 bulkUpdateAssignee）+ `bulkDueDateValue` 信号 + `showBulkActionPanel` 扩 `'due'`；`app.html` 批量栏加「批量改截止日期」按钮 + 日期面板（`<input type="date">` + 应用/清除）；`app.css` 补 `.bulk-date-input`。
  - 构建 `main-45AUETER.js` → cp `frontend/dist/frontend/browser/.` → `agentboard/web/static/`（web 只读挂载即时生效，无需 rebuild）；删旧 `main-BGS3FFRS.js`。
  - 后端改动经 `docker restart agentboard-api-1`（只读挂载 ./agentboard，uvicorn 重启重读 api.py），**未触碰 18001(MCP)/web/28080**。
- **验证**：
  - 后端 pytest `tests/test_epic45_bulk_due_date.py` **4 passed**（set/clear/非法日期拒绝/legacy 路径回归），全部 restore 现场。
  - Playwright E2E `tests/test_epic45_bulk_due_date_e2e.py` **全绿**：admin→/story/25→勾 3 任务→批量改截止日期→填 2026-08-01→应用→API 校验 3 任务 due_date 全变；再清除→恢复 None；**0** pageerror/console/.js+.css 404；测试末还原。
- **提交**：`feat(ui): 前端小优化 + 后端增量 - 任务列表批量改截止日期 (Epic 45 v3.2)` → push origin main。
- **硬约束**：未触碰 18001(MCP)/docker compose 配置/端口映射；排除 data/、autodev.lock、其他 automation 的 memory.md、screenshots、前端 dist 源码（仅提交构建产物 static）。
- **下次可执行**：bulk 五件套齐（status/priority/assignee/due/delete）；可转向「保存筛选预设增强（命名/多预设）」或新需求；旧 due/priority/type E2E 因工具条精简已迁移（v3.1 起）。
