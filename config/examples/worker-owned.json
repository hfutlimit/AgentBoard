{
  "Worker": {
    "Id": "local-worker-a",
    "HistoryDatabasePath": "data/worker-a.db"
  },
  "WorkerOwned": {
    "Enabled": true,
    "ReconcileSeconds": 5,
    "Projects": [{ "ProjectId": 8, "LocalPath": "E:\\Projects\\KnowledgeVault" }],
    "Agents": [
      {
        "Id": "codex-a",
        "Enabled": true,
        "Provider": "codex",
        "PrePrompt": "Read repository guidance, requirements and accepted upstream evidence before working.",
        "PostPrompt": "Verify acceptance criteria and report actual checks, results and evidence before returning the required JSON.",
        "Prompts": {
          "dev": { "Pre": "Reproduce reported bugs before implementing a fix.", "Post": "Run relevant regression tests and commit the bounded change." }
        },
        "WorkKinds": ["proposal", "design", "dev", "qa_review"],
        "Runtime": {
          "Command": "codex",
          "Model": "gpt-5.6-terra",
          "Arguments": ["exec", "--json"],
          "TimeoutMinutes": 30,
          "MaxCapturedOutputChars": 100000
        }
      },
      {
        "Id": "codex-b",
        "Enabled": true,
        "Provider": "codex",
        "PrePrompt": "Stay independent from the upstream implementer and verify evidence.",
        "PostPrompt": "Distinguish verified results from untested assumptions.",
        "Prompts": {
          "qa": { "Pre": "Deploy locally and execute the acceptance tests.", "Post": "Report actual deployment/test steps, outcomes and actionable defects without modifying implementation." }
        },
        "WorkKinds": ["design_review", "dev_review", "qa"],
        "Runtime": {
          "Command": "codex",
          "Model": "gpt-5.6-terra",
          "Arguments": ["exec", "--json"],
          "TimeoutMinutes": 30,
          "MaxCapturedOutputChars": 100000
        }
      }
    ]
  },
  "DurableExecution": { "Enabled": false },
  "AgentBoard": { "ServerUrl": "http://127.0.0.1:8000" },
  "Portal": { "Urls": "http://127.0.0.1:58240" }
}
