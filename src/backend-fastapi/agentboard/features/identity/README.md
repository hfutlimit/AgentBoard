# Identity feature

Owns users, credentials, API keys, profile data, identity services, and
identity-related persistence.  Its services are application use cases and do
not define the HTTP route boundary.

